# Anicca iOS 1.9.3 — Design Spec (v3)

| Meta | Value |
|------|-------|
| 作成日 | 2026-06-04 |
| 改訂 | v1 (7-layer) → v2 (③削除 + newsletter 既存接続 + サインアウト削除 + feedback Netlify) → v3 (C-1/C-2 + I-1〜I-4 + M-1〜M-3) → **v4** (re-review: C-1-RESIDUAL = AC-ZeroTouch を削除行数=0 + 追加<=5 + grep=0 の 3 条機械判定に再定義、 I-5 = AC-④ から @Published 削除して pull-only 統一、 M-4 = DoD 12 件、 M-5 = Maestro UI flow 明確化) |
| 著者 | Anicca (Claude Opus 4.8) + Dais |
| Base | 1.9.2 (build 367 = 1.9.0 IM-feed source fe25e111、 App Store WAITING_FOR_REVIEW) |
| Prior lesson | 1.9.1 (build 366) が IM-feed UX を破壊 → ContentRouterView/aniccaiosApp/FeedRootView core を触ると壊れる |
| 決定方針 | 全 design 決定は Anicca が調査 (IBA) して確定。 Dais は完成 spec を read-approve する |

---

## 1. Overview (What & Why)

### What
Anicca iOS 1.9.3 を 1 リリースで App Store に提出。 1.9.2 の **IM-feed swipe UX を ZERO-TOUCH で維持** したまま、 6 layer を追加 (1.9.1 で予定だった 7 機能から ③ billing banner を削除):

| # | layer | 種類 | 実装位置 | 可視性 |
|---|-------|------|---------|--------|
| ① | iOS PushTokenService.register 修復 | iOS hotfix | AppDelegate.didFinishLaunching + PushTokenService.register | invisible |
| ② | backend affirmation sender source 統一 | backend verify | apps/api (既 main land、 Railway deploy 確認) | invisible |
| ④ | cold-start race fix (pendingQuoteId) | iOS | AppState + AppDelegate.didReceive + FeedRootView.onAppear | invisible (通知タップ時) |
| ⑤ | JA 12 件 humanize | content | apps/api/.../catalog/ja.json | invisible (text content) |
| ⑥ | newsletter = 既存「Daily Anicca Letter」接続 | iOS UI | SettingsSheet Section + 既存 lead-magnet endpoint | visible (Settings 内) |
| ⑦ | feedback form | iOS UI + Netlify | SettingsSheet Section + FeedbackFormView + 新 Netlify function | visible (Settings 内) |

★ **削除した layer**: ③ billing-issue banner (Dais「クソすぎる」)。 FeedRootView の ZStack overlay 追加 = 不要 → IM-feed 破壊 risk が消滅。

### Why

**1.9.1 が live で IM-feed UX を破壊した教訓 (Dais 2026-06-04 確認):**
- 1.9.1 build 366 = iam-feed branch HEAD ベースで、 swipe フィードが旧 UX に逆戻りしていた
- 修正: 1.9.2 で 1.9.0 IM-feed source (fe25e111) を rebuild → WAITING_FOR_REVIEW 提出済 (build 367)
- 1.9.3 の使命: 1.9.1 でやりたかった機能を、 IM-feed swipe 体験を **1 文字も壊さず** に追加

**⑥ newsletter の方向転換 (調査で判明、 Dais 指示):**
- 「アニッチャが毎日オリジナルの手紙を書いて送る」べき (ランダム affirmation メールは飽きる/間違い)
- ★ 既存システムが稼働中 ★ = aniccaai.com/letter「Daily Anicca Letter」($9.99/月、 365 通、 毎朝 ~150 words)
- 仕組み: Supabase `subscribers` テーブル + `daily-letter-sender` cron (06:13 JST、 Resend Batch、 day_offset drip) + `weekly-fresh-letter` cron (gpt-5.4-mini が今週の news×無常 で fresh letter 生成、 飽き防止) + `letter-bank-builder` (365 通 bank 生成)
- ★ iOS は web と同じ `aniccaai.com/.netlify/functions/lead-magnet` を叩くだけ ★ で既存システムに乗る
- 私が前回作った Railway `newsletter.js` (random affirmation) は全廃

### Cited sources (実コード・実システム調査、 2026-06-04)

| Topic | Source | 確認した事実 |
|-------|--------|-------------|
| IM-feed core 構造 | `aniccaios/aniccaios/Views/Feed/FeedRootView.swift` L15-79 | ZStack(.top){ themeStore.image L19-24; VerticalPager{QuoteCardView} L27-34; chrome ⚙ HStack L37-45 }; .onAppear quotes 初期化 L50-54; .onReceive aniccaScrollToQuote warm path L73-78 |
| routing (触らない) | `aniccaios/aniccaios/ContentView.swift` L10-33 | ContentRouterView: onboardingComplete → FeedRootView()。 signedOut/signedIn 両方 同 view |
| サインイン不在 | grep `SignInWithAppleButton`/`ASAuthorizationAppleIDButton` = **0 hits** in active Views; AuthCoordinator.signInWithApple は どこからも呼ばれない (legacy のみ) | サインイン UI は存在しない |
| サインアウト no-op | `SettingsSheet.swift` L62-68 | Button(role:.destructive){ /* Sign out hook — kept minimal */ } = 空 hook、 何もしない死んだボタン |
| Daily Letter LP | https://aniccaai.com/letter | "$9.99/month, 365 letters, one a morning ~150 words, Pali concept + reframe + breath" |
| web signup endpoint | `apps/landing/app/letter/page.tsx` L14 + `netlify/functions/lead-magnet.js` | POST `/.netlify/functions/lead-magnet` { email, lang } → Supabase subscribers upsert { email, lang, signed_up_at } + Day-0 letter 即送信 |
| drip 配信 cron | `~/.openclaw/skills/daily-letter-sender/SKILL.md` | 毎朝 06:13 JST、 subscribers から day_offset 計算、 bank_letter_<lang>.jsonl から該当 letter、 Resend Batch 送信、 free=14日/paid=無制限 |
| fresh content cron | `~/.openclaw/skills/weekly-fresh-letter/SKILL.md` | 毎週日 08:13 JST、 gpt-5.4-mini が今週 news×無常 で fresh letter 生成 → bank 追加 (飽き防止) |
| feedback endpoint pattern | `apps/landing/netlify/functions/lead-magnet.js` | Resend `api.resend.com/emails` POST で送信。 同パターンで feedback.js 新設 |
| APNs sender (① ② の前提) | Railway API service log (Dais: 36分前に通知来た) | AffirmationApnsSenderJob 稼働中、 register 経路のみ要修復 |

---

## 2. Acceptance Criteria

全 MUST。 OR 禁止。

| # | Criterion | Verification |
|---|-----------|-------------|
| AC-ZeroTouch | FeedRootView.swift の **body プロパティ宣言 (L9-13) と ZStack 内 3 layer (themeStore.image / VerticalPager / chrome HStack) が 1 行も変更されていない**。 ④ の追加は `.onAppear` クロージャ末尾の consume ブロック (4 行: `if let qid...{` / `withAnimation { currentIndex = idx }` / `}` の 3-4 行) のみ | (機械判定 3 条すべて MUST) **(a)** `git diff release/1.9.2-revert -- FeedRootView.swift --stat` で **削除行数 (`-`) = 0** (既存行は 1 行も消えない/変わらない)。 **(b)** 追加行数 (`+`) <= 5 (consume ブロックのみ)。 **(c)** `grep -c '@ObservedObject\|@EnvironmentObject' FeedRootView.swift` = **0** (appState 購読を増やしていない)。 ★ 削除行 0 が ZERO-TOUCH の本質 = 既存 core は触らず追加のみ |
| AC-① | `AppDelegate.didFinishLaunching` で `registerForRemoteNotifications()` **無条件呼出** (旧 `if authorized` gate 除去)、 `applicationDidBecomeActive` で再呼出、 `PushTokenService.register` が 3 retry (0s/1s/2s) + 最終失敗で UserDefaults `pushTokenRegistrationPending` flag | XCTest `test_didFinishLaunching_callsRegister()` + `test_register_retriesThenSetsFlag()` |
| AC-② | `apps/api/src/jobs/problemNudgeApnsSenderJob.js` (logger context = `AffirmationApnsSenderJob`、 M-1 確認) が affirmation sender 版 (title=`アニッチャ`/body=affirmation/quoteId) で Railway production 稼働。 ※ 成功は `{sent:N}` return で追跡され明示 "sent ok" log は無い | vitest `problemNudgeApnsSenderJob.test.js` で payload (title/body/quoteId) assert + Railway log に `[AffirmationApnsSenderJob]` context 行が出ている (稼働証明、 Dais: 36分前通知 OK) + DB `pushToken` に 1.9.3 install 後の新 register 行が現れる (= ① と連動) |
| AC-④ | 通知タップ (cold-launch/warm 両方) で Feed の **同一 quoteId** カードに scroll。 AppState.pendingQuoteId は **plain `var` (★ @Published 不可 — C-2 pull-only)** + consumePendingQuoteId() atomic (@MainActor、 defer で get-and-clear)。 既存 warm path (.onReceive) 維持 | XCTest `test_AppState_consumePendingQuoteId_atomic()` + Maestro `v1.9.3/01-cold-launch.yaml` |
| AC-⑤ | `apps/api/src/modules/affirmations/catalog/ja.json` の q024/050/066/105/112/125/135/136/142/162/186/190 が humanizer-ja `scripts/score.sh` で 全 `ai_likelihood <= 30` | `bash score.sh ja.json q024 ... q190 \| jq -e 'all(.[]; .ai_likelihood <= 30)'` = true |
| AC-⑥ | Settings 内 `accessibilityIdentifier("newsletter-email-field")` に valid email → `newsletter-submit-button` tap → `POST https://aniccaai.com/.netlify/functions/lead-magnet { email, lang }` で **HTTP 200 + body `{ok:true}`** → 登録後は `newsletter-status-text` "毎朝の手紙を登録しました" 表示。 ★ **lang は MUST: AppState.effectiveLanguage が `.ja` の時 文字列 `"jp"` を送る (`"ja"` 送信は禁止 — lead-magnet は不正 lang を黙って en にフォールバックし日本語ユーザーに英語 letter が届く)。 `.en` 等は `"en"`** | (機械判定) iOS の `newsletterLang(for:)` 純粋関数を XCTest で `.ja → "jp"` / `.en → "en"` assert (Test #5.5) + endpoint 200+{ok:true} を Maestro。 (補助確認) Dais Gmail で Day-0 letter 受信 (= Resend delivered log) |
| AC-⑦ | Settings 内 `feedback-section` → NavLink → `feedback-textarea` (5-2000 chars) + `feedback-submit-button` tap → `POST https://aniccaai.com/.netlify/functions/feedback { text, locale, appVersion }` で **HTTP 200 + body `{ok:true}`** → feedback.js が Resend `api.resend.com/emails` を呼ぶ | (機械判定) feedback.js vitest で Resend mock の引数が `to==="user@example.com"` を assert (Test #6) + text>2000→413 (Test #7) + endpoint 200 を Maestro。 (補助確認) Dais Gmail 受信 (= Resend delivered log) |
| AC-NoSignOut | SettingsSheet に「サインアウト」Section が **存在しない** (削除済)。 `settings_sign_out` の Localizable key は dead key として残して可 (削除すると他参照 risk、 M-3) | `grep settings_sign_out SettingsSheet.swift` = 0 hits |
| AC-Regress | IM-feed 既存機能 (swipe / like ♡ / copy ❐ / share ↑ / theme picker / settings ⚙) が regression なし | Maestro `v1.9.3/04-imfeed-regression.yaml` |
| AC-Build | `xcodebuild build -scheme aniccaios -destination "id=$UDID" -configuration Debug` = `** BUILD SUCCEEDED **` | exit 0 |
| AC-Test-iOS | `xcodebuild test` 全 XCTest pass | `** TEST SUCCEEDED **` |
| AC-Test-API | `cd apps/api && npm test` baseline pass | vitest exit 0 |
| AC-E2E | `maestro test aniccaios/Maestro/v1.9.3/` 全 flow pass | exit 0 |
| AC-Verify | 各 layer 実装後、 Dais が Xcode で Cmd+R Run → Simulator で手動 verify (6 layer × 1 回) | Dais OK |
| AC-Submit | App Store Connect で 1.9.3 = `WAITING_FOR_REVIEW`、 build 368+ | `asc versions view --version-id <1.9.3>` で `WAITING_FOR_REVIEW` |

---

## 3. As-Is / To-Be

### ① iOS PushTokenService.register 修復

| | As-Is (1.9.0/1.9.2) | To-Be (1.9.3) |
|---|---------------------|----------------|
| didFinishLaunching | `await registerForRemoteIfAuthorized()` (`if authorized` gate) | `await registerForRemoteNotifications()` 無条件 + pending flag retry |
| applicationDidBecomeActive | `registerForRemoteIfAuthorized()` | `registerForRemoteNotifications()` 無条件 |
| PushTokenService.register | 単発 POST、 retry なし | `postOnce` を 3 retry (delays [0, 1s, 2s])、 全失敗で `pushTokenRegistrationPending` flag |

### ② backend source 統一 (verify のみ)

| | As-Is | To-Be |
|---|-------|-------|
| main affirmation sender | 既 land (title=アニッチャ/body=affirmation/quoteId) | 変化なし — Railway 稼働確認 (Dais: 36分前通知 OK) |

### ④ Cold-start race fix (★ pull-only 方式 = reviewer C-2 反映)

★ **設計核心 (C-2)**: FeedRootView に `@ObservedObject appState` を **追加しない**。 追加すると pendingQuoteId 変化で body 全体 (VerticalPager 含む) が再描画され、 1.9.1 型の挙動変化 risk。 代わりに `.onAppear` 内で `AppState.shared.consumePendingQuoteId()` を **一度だけ pull** する (購読しない)。 これで body の再描画グラフを一切変えない。

| | As-Is | To-Be |
|---|-------|-------|
| AppState | pendingQuoteId なし | `var pendingQuoteId: String? = nil` (★ `@Published` **不要** — FeedRootView は購読せず pull するだけ) + `consumePendingQuoteId() -> String? { defer { pendingQuoteId = nil }; return pendingQuoteId }` (@MainActor) |
| AppDelegate.didReceive | aniccaScrollToQuote post のみ | post 維持 (warm path) + `Task { @MainActor in AppState.shared.pendingQuoteId = quoteId }` (cold-launch queue) |
| AppDelegate.didFinishLaunching | — | `#if DEBUG` launch arg/env parse (PENDING_QUOTE_ID_ON_LAUNCH) for Maestro |
| FeedRootView.onAppear | quotes 初期化のみ (L50-54) | 末尾に `if let qid = AppState.shared.consumePendingQuoteId(), let idx = quotes.firstIndex(where: {$0.id == qid}) { withAnimation { currentIndex = idx } }` 追加 (★ `AppState.shared` を直接 pull、 プロパティ購読なし)。 ★ 既存 quotes 初期化 不変 ★ |
| FeedRootView の body プロパティ | appState 参照なし | ★ **変更なし** — `@ObservedObject` も `@EnvironmentObject` も追加しない。 onAppear クロージャ内で `AppState.shared` を呼ぶだけ (再描画グラフ不変) |

### ⑤ JA 12 件 humanize

| | As-Is | To-Be |
|---|-------|-------|
| ja.json (1.9.0 直訳版) | q024「静けさは私の故郷であり、私の母国語です。」等 | humanize (q024「静けさが、 家のような気がする。」等)、 全 ai_likelihood <= 30 |

### ⑥ Newsletter = 既存「Daily Anicca Letter」接続

| | As-Is | To-Be |
|---|-------|-------|
| SettingsSheet | Section{Theme/Notif/Liked} + Section{Subscription} + Section{SignOut} | SignOut 削除 + 末尾に NewsletterSection 追加 |
| NewsletterSection | なし | TextField(newsletter-email-field) + Button(newsletter-submit-button)。 tap で `POST https://aniccaai.com/.netlify/functions/lead-magnet { email, lang }`。 成功で `newsletter-status-text` "毎朝の手紙を登録しました" |
| backend | 既存稼働 (web と共有) | iOS 側は endpoint を叩くだけ。 daily-letter-sender / weekly-fresh-letter cron が既存のまま全部やる |
| lang | — | AppState.effectiveLanguage が ja → "jp"、 それ以外 → "en" (lead-magnet の lang 仕様に合わせる) |

### ⑦ Feedback form

| | As-Is | To-Be |
|---|-------|-------|
| SettingsSheet | (同上) | NewsletterSection の後に FeedbackSection 追加 |
| FeedbackSection | なし | NavLink → FeedbackFormView (TextEditor feedback-textarea 5-2000 + Button feedback-submit-button + PII 警告 alert) |
| endpoint | なし | 新 Netlify function `apps/landing/netlify/functions/feedback.js`: POST { text, locale, appVersion } → Resend `api.resend.com/emails` で from=anicca-feedback@aniccaai.com to=user@example.com。 lead-magnet.js と同パターン。 iOS は `POST https://aniccaai.com/.netlify/functions/feedback` |
| ★ 既存 Railway route を使わない根拠 (I-4) | — | Railway に `POST /api/mobile/nudge/feedback` が既存 (nudge.js L438) だが、 これは **特定 nudge の評価専用** (extractUserId + resolveProfileId + feedbackSchema で nudge 評価を記録)。 汎用 app フィードバックではない。 汎用 feedback を Netlify に置く理由: ①newsletter (⑥) と同じ aniccaai.com Netlify + Resend インフラで Gmail 直送が即できる ②Railway nudge route の schema/認証を汎用化すると既存 nudge 評価機能を壊す risk ③Netlify は git deploy 可で Railway auto-deploy 問題 (#47) を回避。 → backend は Railway (push/affirmation = ①②⑤) + Netlify (newsletter/feedback = ⑥⑦) + Supabase (subscribers) の 3 系統だが、 各々が既存システムの自然な延長で、 新規結線を最小化する選択 |

### ② SettingsSheet サインアウト削除

| | As-Is | To-Be |
|---|-------|-------|
| SettingsSheet L62-68 | `Section { Button(role:.destructive){ /* no-op */ } label: { Label(settings_sign_out) } }` | **Section 丸ごと削除** (サインイン UI 不在、 死んだボタン) |

---

## 4. Test Matrix

| # | To-Be | Test Name | Type | Cover |
|---|-------|-----------|------|-------|
| 1 | ① didFinishLaunching 無条件 register | `test_AppDelegate_didFinishLaunching_callsRegister()` | XCTest | OK |
| 2 | ① PushTokenService 3 retry + flag | `test_PushTokenService_register_retriesThenSetsFlag()` | XCTest | OK |
| 3 | ④ AppState.consumePendingQuoteId atomic | `test_AppState_consumePendingQuoteId_atomic()` | XCTest | OK |
| 4 | ② sender title/body/quoteId | `test_AffirmationApnsSender_payload()` | vitest | OK |
| 5 | ⑤ JA 12 ai_likelihood <= 30 | `test_ja12_humanizer_score()` | vitest + score.sh | OK |
| 5.5 | ⑥ lang 変換 `.ja → "jp"` / `.en → "en"` (I-1) | `test_newsletterLang_jaMapsToJp()` | XCTest (純粋関数) | OK |
| 6 | ⑦ feedback.js Resend mock `to===user@example.com` (I-2) | `test_feedback_function_sendsToDais()` | vitest (function unit, Resend mock) | OK |
| 7 | ⑦ feedback.js text > 2000 → 413 | `test_feedback_function_tooLong()` | vitest | OK |
| 8 | ④ Maestro cold-launch scroll | `v1.9.3/01-cold-launch.yaml` | Maestro | OK |
| 9 | ⑥ Maestro newsletter UI flow (入力→submit→`newsletter-status-text` 表示) | `v1.9.3/02-newsletter.yaml` | Maestro (UI flow + status-text。 200+{ok:true} の機械判定は #5.5 + endpoint curl) | OK |
| 10 | ⑦ Maestro feedback UI flow (入力→submit→sent 表示) | `v1.9.3/03-feedback.yaml` | Maestro (UI flow。 機械判定は #6/#7 vitest) | OK |
| 11 | AC-Regress IM-feed swipe/like/settings/theme | `v1.9.3/04-imfeed-regression.yaml` | Maestro | OK |

★ 全 12 OK 必須。

---

## 5. Boundaries (Out of Scope) — ★ ZERO-TOUCH 原則 ★

| Out / 禁止 | 理由 |
|-----------|------|
| **`ContentRouterView` (ContentView.swift) の routing 変更** | 1.9.1 が壊した最有力候補 |
| **`aniccaiosApp.swift` の WindowGroup / onOpenURL 変更** | エントリポイント不変 |
| **`FeedRootView.swift` の body プロパティ宣言 (L9-13) + ZStack 内 3 layer (L19-45) の変更** | IM-feed core。 ④ の追加は **`.onAppear` クロージャ末尾の consume 行のみ**。 ★ `@ObservedObject` / `@EnvironmentObject` 等のプロパティ購読は **追加禁止** (C-2: 再描画グラフを変えない pull-only 方式)。 onAppear 内で `AppState.shared` を直接呼ぶ |
| **`QuoteCardView.swift` / `VerticalPager.swift` 変更** | swipe カード・paging engine 不変 |
| **OnboardingFlowView 変更** | onboarding 不変 |
| **③ billing-issue banner** | Dais「クソすぎる」→ 実装しない (FeedRootView ZStack overlay 追加 = 不要) |
| Newsletter の独自配信ロジック (Railway random affirmation) | 既存「Daily Anicca Letter」に乗るので不要、 全廃 |
| iOS streak 機能 | NEVER (IAM benchmark で retention 殺す) |
| Android | 据置 |

---

## 6. Execution Steps

```bash
# STAGE 0: worktree = feature/anicca-1.9.3 off release/1.9.2-revert (clean IM-feed base)
cd /Users/anicca/anicca-1.9.2-revert
git checkout -b feature/anicca-1.9.3 release/1.9.2-revert
UDID=$(xcrun simctl list devices available | grep "iPhone 17" | head -1 | sed -E 's/.*\(([A-F0-9-]{36})\).*/\1/')

# STAGE 4a (⑤ JA humanize): ja.json 12 件 → score.sh で全 <= 30
# STAGE 4b (② backend verify): Railway affirmation sender 稼働確認 + ja.json deploy
# STAGE 4c (① register fix): AppDelegate + PushTokenService → XCTest #1,#2 → Dais Run
# STAGE 4d (④ cold-start race): AppState + AppDelegate.didReceive + FeedRootView.onAppear → XCTest #3 → Maestro #8 → ZERO-TOUCH git diff → Dais Run
# STAGE 4e (⑦ feedback): feedback.js Netlify + SettingsSheet FeedbackSection → vitest #6,#7 → Maestro #10 → Dais Run
# STAGE 4f (⑥ newsletter): SettingsSheet NewsletterSection (lead-magnet endpoint) + SignOut 削除 → Maestro #9 → Dais Run

# STAGE 5: code review (architect spec compliance → quality)
# STAGE 6: maestro test aniccaios/Maestro/v1.9.3/ (全 + regression)
# STAGE 7: version 1.9.3 / build 368、 asc review submit → WAITING_FOR_REVIEW
```

---

## 7. E2E Judgment

| Item | Value |
|------|-------|
| UI 変更 | あり (Settings 2 section 追加・1 section 削除、 cold-start scroll) |
| 結論 | **Maestro: MUST** (cold-start race + regression は手動回帰不能) |
| Maestro flow | `v1.9.3/01-cold-launch.yaml` `02-newsletter.yaml` `03-feedback.yaml` `04-imfeed-regression.yaml` |
| iOS unit | XCTest 3 件 |
| backend unit | vitest 4 件 (sender payload + JA score + feedback.js happy/tooLong) |
| ★ 各 layer 後 | Dais Xcode Cmd+R Run (Apple sign-in 要、 fastlane IPA 直 install 不可) |

---

## 8. User GUI Tasks (Dais 関与 必要)

| When | Task | 完了確認 |
|------|------|---------|
| STAGE 4 各 layer 後 | Xcode で feature/anicca-1.9.3 を Cmd+R Run → Simulator 手動 verify (通知/newsletter/feedback) | Dais OK |
| STAGE 7 後 | App Store 1.9.3 審査結果プッシュ受領 | Apple email |

---

## 9. Risks & Mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| ④ で FeedRootView 触り 1.9.1 再来 | IM-feed 再破壊 | AC-ZeroTouch で L19-45 unchanged を git diff verify + ④ 単独 Dais Run。 壊れたら ④ だけ revert |
| ⑥ lead-magnet endpoint が iOS POST を拒否 | newsletter 登録不可 | iOS の `URLSession` ネイティブ POST はブラウザでないため CORS preflight 非対象 → 動作する (I-3 確認: lead-magnet.js に CORS ヘッダは無いが iOS では問題なし)。 ただし endpoint は web (`/letter` page) と共有なので、 将来 web クロスオリジン呼出が入ったら別途 CORS 対応要 (iOS のみなら現状で可) |
| ⑦ feedback.js が Netlify auto-deploy されない | endpoint 404 | aniccaai.com Netlify は手動 deploy (memory)、 `netlify deploy --prod` で ship + curl 確認 |
| ⑤ ja.json humanize が Railway catalog に届かない | 通知の JA が直訳のまま | Railway deploy verify (#47)。 affirmation sender が新 ja.json 読むか確認 |
| 1.9.2 reject → 1.9.3 base 無効 | 提出やり直し | 1.9.3 は 1.9.2 source + additive。 1.9.2 reject 理由次第で 1.9.3 が代替提出 |

---

## 10. Definition of Done

| # | DoD | Owner |
|---|-----|-------|
| 1 | AC-ZeroTouch 〜 AC-Submit 全 green | Anicca + Dais |
| 2 | Test Matrix 12 件 全 OK | Anicca |
| 3 | 各 layer Dais Xcode Run verify OK (6 回) | Dais |
| 4 | spec/plan が docs/superpowers/{specs,plans}/2026-06-04-anicca-1.9.3*.md に commit | Anicca |
| 5 | git: feature/anicca-1.9.3 → review → merge → release/1.9.3 tag | Anicca |
| 6 | App Store 1.9.3 = WAITING_FOR_REVIEW (asc CLI) | Anicca |
