# HANDOFF — Anicca (telephony + product-redesign patches) — 2026-06-16

> You are the next agent. **Do NOT start implementing immediately.** First do steps A→B→C below and present them IN CHAT to Dais. Only after he confirms, implement. Read the cited files to grasp state — they are the source of truth.

## 0. The mission (unchanged)
Anicca = a self-funding autonomous agent that (1) **earns money** for the user with **no human in the loop**, and (2) **manages the user's life** (calendar, travel, phone reminders). Two distributions: **OSS local** (free, runs on user's device) + **Cloud web app** (aniccaai.com). It must be shippable as a real SaaS — anyone signs up, their own Anicca runs, earns, and manages their life, and **calls them on the phone in ANY country**.

## 1. SOURCE-OF-TRUTH FILES (read these first)
| What | Path |
|---|---|
| Product redesign spec (canonical) | `docs/superpowers/specs/anicca/28-product-redesign-merge-2026-06-16.md` |
| Telephony design | `docs/superpowers/specs/2026-06-16-life-call-telnyx-charon-design.md` |
| The 5 reviewed patches | `docs/superpowers/specs/anicca/patches/P-{install-me-flow,lm-separate,oss-local,malice-guard,lm-local-calling}.patch.md` (secured on `dev` commit `dff85249`; backups in `/tmp/anicca-patches-backup/`) |
| Telnyx Charon call runner | `apps/landing/scripts/life-call-telnyx.mjs` + bridge `apps/landing/scripts/call-bridge.cjs` |
| Repos | products `~/anicca-project` → github `Daisuke134/anicca-products`; OSS mother `~/anicca` → github `Daisuke134/anicca`; live runtime `~/.openclaw` + `~/clawd` |
| Secrets (NEVER commit) | `~/.openclaw/.env` — `TELNYX_API_KEY`, `TELNYX_CONNECTION_ID`/CC-app `2982013078364751402`, `TELNYX_PUBLIC_KEY`, `GEMINI_API_KEY`, Supabase keys, `SIGNALWIRE_*` |

## 2. CURRENT STATE (2026-06-16, honest)

### Telephony — BREAKTHROUGH but call not yet confirmed ringing
- The universal blocker all session = every carrier's anti-toll-fraud gate (Twilio err 21216 fraud-block on Dais's number; Telnyx D60 trial; SignalWire 21219 trial). Exhaustive 4-cluster web research confirmed: **no platform puts Gemini Live "Charon" on a phone (all lock to their own voice) → you MUST self-bridge Media-Stream ⇄ Gemini Live**; and **no gate-free global path exists** (toll-fraud reality) — one-time fund+verify is universal.
- **RESOLVED: Telnyx gate is LIFTED.** With Dais's new `TELNYX_API_KEY` + his funding ($9.73 bal) + verification, a real `POST /v2/calls` to `+81XXXXXXXXXX` via CC-app `2982013078364751402` returned a `call_control_id` with **no 10010/error**. Telnyx can now dial any country.
- **OPEN:** `life-call-telnyx.mjs` was launched (cloudflared tunnel → bridge → `/v2/calls` stream_url + `stream_bidirectional_mode=rtp` → Gemini Live Charon → record). NOT yet confirmed that Dais's phone rang + Charon spoke. **Likely fix needed:** `call-bridge.cjs` was written for **Twilio Media Streams (μ-law base64 over WS)**; Telnyx streams **RTP/L16** — verify/adapt the bridge's media decode/encode for Telnyx's frame format, then re-run until the phone rings with audio. Verify E2E: real Call-SID + ring/answer + recording with audible Charon (HARD 0.31).

### Patches — 5/5 reviewed (superpowers ok:true) + applied; NOT deployed/E2E
| patch | review | applied | live E2E |
|---|---|---|---|
| install-me-flow (/install "Get started free"→/me auth-gated, $5/$30, trial) | ✅ ok:true | **PR #60→main** (npm build EXIT=0) | ❌ not merged/deployed; Google login needs Supabase config (Google provider + `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY`); currently fails-honest "ログイン準備中" |
| lm-separate (/lm product, Composio gcal+gmail, $20/mo) | ✅ ok:true | **PR #61→main** (build EXIT=0) | ❌ not merged; **SECURITY task #39 must be fixed before merge** (IDOR/open-redirect/unauth OAuth in lm-onboard/gmail-connect/calendar-connect — read the finding) + needs Composio gmail toolkit + `lm_users` table |
| oss-local (~/anicca Franklin-style local) | ✅ ok:true | OSS main `0f97fcc` | ❌ not synced to live `~/clawd` |
| lm-local-calling (Telegram live-loc call cadence) | ✅ ok:true | ~/anicca `e9b54c7` (14/14) | ❌ not synced to `~/clawd`; no registry/cron wiring |
| malice-guard (earn≠user-PII) | ✅ ok:true | ~/anicca `3f14c0f` (24/24) | ❌ ~/clawd sync needs run.sh `env -i` allowlist first or it HALTS live earn loop |

### Other live-verified earlier this session (context)
stripe-spawn (real DO droplet create→destroy), install $30 button, dashboard real numbers, life-ask/life-notify real mail round-trips, self-spawn (real child born→destroyed), earn x402-serve endpoint live. See STATUS.md.

## 3. WHAT THE NEXT AGENT MUST DO (in order)
**A. Grasp + organize the task list** (present in chat): read §1 files, reconcile with TaskList (#34–39 + telephony), output a clean ordered task list of what remains and HOW (commands/files), separating: (1) ring-the-phone (Telnyx RTP bridge), (2) merge+deploy PRs #60/#61 after security fix, (3) Supabase/Composio config for live auth, (4) ~/clawd sync of OSS skills (with malice-guard env-scrub), (5) UX polish with taste-skill + ui-ux-pro-max-skill.
**B. Full architecture** (present in chat, complete): the 3 product lines + the telephony stack (Telnyx CC-app → RTP media WS → call-bridge.cjs → Gemini Live Charon) + earn + life-manager + malice-guard boundary.
**C. Full post-implementation UX ASCII** (present in chat): /install→/me (cloud earner), OSS-local, /lm (life manager), and the phone-call experience — for BOTH local and cloud.
**Then** await Dais's go, then implement + verify E2E (HARD 0.24/0.31: no fake, real run, real evidence). Do NOT delegate the phone call to a sub-agent — run it yourself.

## 4. Constraints
Secrets only in `~/.openclaw/.env`. No fake/dry-run. Commit author for products = `Daisuke Sato <user@example.com>` (lefthook). Branch prefixes `feature/|fix/|chore/|docs/`. PRs to `main`; products deploy from `main` (Netlify). Don't email real third parties in tests.
