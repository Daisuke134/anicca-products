# Anicca iOS 1.9.3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 1.9.2 (1.9.0 IM-feed source) の swipe フィード UX を ZERO-TOUCH で維持したまま 6 layer (① register / ② sender verify / ④ cold-start race / ⑤ JA humanize / ⑥ newsletter / ⑦ feedback) を追加し、 1.9.3 を App Store WAITING_FOR_REVIEW に提出する。

**Architecture:** iOS 側は `FeedRootView` の core (themeStore.image / VerticalPager / chrome ⚙) を **1 行も削除/変更せず**、 追加のみ。 ④ cold-start は `@ObservedObject` を増やさず `.onAppear` で `AppState.shared` を pull する pull-only 方式。 ⑥ newsletter は既存「Daily Anicca Letter」(aniccaai.com Netlify lead-magnet → Supabase subscribers → daily-letter-sender cron) に iOS が接続。 ⑦ feedback は新 Netlify function → Resend → Dais Gmail。 backend は Railway (①②⑤) + Netlify (⑥⑦) + Supabase (subscribers) の 3 系統。

**Tech Stack:** Swift 5 / SwiftUI / XCTest / XCUITest / Maestro / Node.js / vitest / Netlify Functions / Resend / asc CLI

**Spec:** [docs/superpowers/specs/2026-06-04-anicca-1.9.3-design.md](../specs/2026-06-04-anicca-1.9.3-design.md) v4 (commit 1ead7b8c、 code-reviewer Ready to proceed)

**Worktree base:** `/Users/anicca/anicca-1.9.2-revert`、 branch = `feature/anicca-1.9.3` off `release/1.9.2-revert`

**ZERO-TOUCH 原則 (絶対):** 触らない file = ContentView.swift (ContentRouterView) / aniccaiosApp.swift / FeedRootView.swift の body プロパティ宣言 + ZStack 内 3 layer / QuoteCardView.swift / VerticalPager.swift / OnboardingFlowView。 各 iOS layer 後に AC-ZeroTouch 3 条 (削除行=0 / 追加<=5 / grep @ObservedObject=0) を確認。

---

## File Map

### iOS (`/Users/anicca/anicca-1.9.2-revert/aniccaios`)

| File | Action | Responsibility |
|------|--------|----------------|
| `aniccaios/AppDelegate.swift` | Modify | ① didFinishLaunching 無条件 register + pending flag retry / applicationDidBecomeActive / ④ didReceive で pendingQuoteId set / #if DEBUG launch arg parse |
| `aniccaios/AppState.swift` | Modify | ④ pendingQuoteId plain var + consumePendingQuoteId() / ⑥ newsletterLang(for:) static 純粋関数 |
| `aniccaios/Services/PushTokenService.swift` | Modify | ① register() 3 retry + pending flag |
| `aniccaios/Views/Feed/FeedRootView.swift` | Modify (additive only) | ④ .onAppear 末尾に consume 行追加 (★ 削除行0) |
| `aniccaios/Views/Feed/SettingsSheet.swift` | Modify | ⑥⑦ NewsletterSection + FeedbackSection 追加 / ② サインアウト Section 削除 |
| `aniccaios/Resources/{en,ja}.lproj/Localizable.strings` | Modify | newsletter_* / feedback_* keys |
| `aniccaios/Resources/{es,fr,de,pt-BR}.lproj/Localizable.strings` | Modify | en コピー (英語 fallback) |
| `apps/api/src/modules/affirmations/catalog/ja.json` | Modify | ⑤ 12 件 humanize |
| `aniccaiosTests/RegisterPathTests.swift` | Create | XCTest #1 #2 (① register) |
| `aniccaiosTests/ColdStartRaceTests.swift` | Create | XCTest #3 (④ consumePendingQuoteId) |
| `aniccaiosTests/NewsletterLangTests.swift` | Create | XCTest #5.5 (⑥ lang ja→jp) |
| `Maestro/v1.9.3/01-cold-launch.yaml` | Create | Maestro #8 |
| `Maestro/v1.9.3/02-newsletter.yaml` | Create | Maestro #9 |
| `Maestro/v1.9.3/03-feedback.yaml` | Create | Maestro #10 |
| `Maestro/v1.9.3/04-imfeed-regression.yaml` | Create | Maestro #11 |

### Netlify (`apps/landing`)

| File | Action | Responsibility |
|------|--------|----------------|
| `apps/landing/netlify/functions/feedback.js` | Create | ⑦ POST {text,locale,appVersion} → Resend → user@example.com |
| `apps/landing/netlify/functions/__tests__/feedback.test.js` | Create | vitest #6 #7 |
| `apps/landing/package.json` | Modify | vitest devDep + test script |

### Backend verify (`apps/api`)

| File | Action | Responsibility |
|------|--------|----------------|
| `apps/api/src/jobs/__tests__/problemNudgeApnsSenderJob.test.js` | Verify/Create | vitest #4 (② payload) |

---

## Task 0: Worktree setup

**Files:** worktree only

- [ ] **Step 0.1: branch off 1.9.2 base**

```bash
cd /Users/anicca/anicca-1.9.2-revert
git checkout release/1.9.2-revert
git checkout -b feature/anicca-1.9.3
git branch --show-current   # → feature/anicca-1.9.3
```

- [ ] **Step 0.2: UDID + baseline build**

```bash
UDID=$(xcrun simctl list devices available | grep "iPhone 17" | head -1 | sed -E 's/.*\(([A-F0-9-]{36})\).*/\1/')
echo "UDID=$UDID"
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
xcodebuild build -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" -configuration Debug 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 0.3: baseline vitest**

```bash
cd /Users/anicca/anicca-1.9.2-revert/apps/api
npm test 2>&1 | tail -5
```

Expected: vitest baseline pass

---

## Task 1 (STAGE 4a): ⑤ JA 12 件 humanize

**Files:** Modify `apps/api/src/modules/affirmations/catalog/ja.json`

- [ ] **Step 1.1: before score 確認**

```bash
cd /Users/anicca/anicca-1.9.2-revert
SCORE=/Users/anicca/anicca-project/.claude/skills/humanizer-ja/scripts/score.sh
JA=apps/api/src/modules/affirmations/catalog/ja.json
bash "$SCORE" "$JA" q024 q050 q066 q105 q112 q125 q135 q136 q142 q162 q186 q190 | jq '[.[] | {id, ai_likelihood}]'
```

- [ ] **Step 1.2: 12 件 humanize 適用**

```bash
cd /Users/anicca/anicca-1.9.2-revert
JA=apps/api/src/modules/affirmations/catalog/ja.json
jq '.quotes.q024 = "静けさが、 家のような気がする。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q050 = "いま、 ここにいる。 それだけで十分。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q066 = "むずかしい時こそ、 手のひらを ひらいて。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q105 = "恐れが やってきても、 家の主は わたしのまま。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q112 = "手を ゆるめれば、 ものごとが 向こうから来る。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q125 = "自分のまま でいるのが、 いちばん 役に立つ。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q135 = "しあわせは、 がんばって 手に入れるものではない。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q136 = "ちいさな おどろきが、 一日の角を まるくする。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q142 = "考えごとは、 水の底に ゆっくり 沈ませて。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q162 = "証明しなくていい。 稼がなくていい。 今日も ここにいる。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q186 = "癒えていく自分に、 やさしく。 その速さは、 その人だけのもの。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq '.quotes.q190 = "ちょっとだけ 満たされた朝。 それで 立ち上がる。"' "$JA" > /tmp/j && mv /tmp/j "$JA"
jq empty "$JA" && echo "JSON OK"
```

- [ ] **Step 1.3: AC-⑤ verify (全 <= 30)**

```bash
cd /Users/anicca/anicca-1.9.2-revert
SCORE=/Users/anicca/anicca-project/.claude/skills/humanizer-ja/scripts/score.sh
JA=apps/api/src/modules/affirmations/catalog/ja.json
bash "$SCORE" "$JA" q024 q050 q066 q105 q112 q125 q135 q136 q142 q162 q186 q190 | jq -e 'all(.[]; .ai_likelihood <= 30)'
```

Expected: `true`

- [ ] **Step 1.4: vitest #5 (humanizer score as test)**

Create `apps/api/src/modules/affirmations/__tests__/ja.humanizer.test.js`:

```javascript
import { describe, it, expect } from 'vitest';
import { execSync } from 'node:child_process';
import path from 'node:path';

describe('JA 12 humanize (Test #5)', () => {
  it('all 12 target quotes have ai_likelihood <= 30', () => {
    const score = '/Users/anicca/anicca-project/.claude/skills/humanizer-ja/scripts/score.sh';
    const ja = path.resolve('src/modules/affirmations/catalog/ja.json');
    const ids = ['q024','q050','q066','q105','q112','q125','q135','q136','q142','q162','q186','q190'];
    const out = execSync(`bash ${score} ${ja} ${ids.join(' ')}`).toString();
    const scores = JSON.parse(out);
    expect(scores).toHaveLength(12);
    for (const s of scores) expect(s.ai_likelihood).toBeLessThanOrEqual(30);
  });
});
```

- [ ] **Step 1.5: Run vitest #5**

```bash
cd /Users/anicca/anicca-1.9.2-revert/apps/api
npm test -- ja.humanizer 2>&1 | tail -8
```

Expected: PASS

- [ ] **Step 1.6: Commit**

```bash
cd /Users/anicca/anicca-1.9.2-revert
git add apps/api/src/modules/affirmations/catalog/ja.json apps/api/src/modules/affirmations/__tests__/ja.humanizer.test.js
git commit -m "fix(content): ⑤ humanize 12 JA affirmations (all ai_likelihood<=30) — 1.9.3 layer 1/6 [Test #5]"
```

---

## Task 2 (STAGE 4b): ② backend affirmation sender verify

**Files:** Verify `apps/api/src/jobs/problemNudgeApnsSenderJob.js` (logger context = AffirmationApnsSenderJob); Create vitest #4

- [ ] **Step 2.1: payload 構造を実コードで確認**

```bash
cd /Users/anicca/anicca-1.9.2-revert
grep -n "title\|body\|quoteId\|アニッチャ\|appName\|affirmationText" apps/api/src/jobs/problemNudgeApnsSenderJob.js | head -15
```

(title=`アニッチャ` / body=affirmation / payload.quoteId を送る箇所を特定)

- [ ] **Step 2.2: vitest #4 RED — sender payload**

Create `apps/api/src/jobs/__tests__/affirmationSenderPayload.test.js`:

```javascript
import { describe, it, expect } from 'vitest';
import fs from 'node:fs';
import path from 'node:path';

describe('② AffirmationApnsSender payload (Test #4)', () => {
  it('source file uses affirmation sender (logger context AffirmationApnsSenderJob + quoteId payload)', () => {
    const src = fs.readFileSync(
      path.resolve('src/jobs/problemNudgeApnsSenderJob.js'), 'utf-8'
    );
    // logger context = AffirmationApnsSenderJob (M-1 確認)
    expect(src).toMatch(/withContext\(['"]AffirmationApnsSenderJob['"]\)/);
    // quoteId を payload に含む
    expect(src).toMatch(/quoteId/);
  });
});
```

- [ ] **Step 2.3: Run #4**

```bash
cd /Users/anicca/anicca-1.9.2-revert/apps/api
npm test -- affirmationSenderPayload 2>&1 | tail -6
```

Expected: PASS (既 land してるので GREEN、 RED にならなければそのまま)

- [ ] **Step 2.4: Railway 稼働確認**

```bash
railway logs --service API --environment production 2>&1 | grep "AffirmationApnsSenderJob" | tail -3
```

Expected: `[AffirmationApnsSenderJob]` context 行が出ている (稼働証明)

- [ ] **Step 2.5: Commit**

```bash
cd /Users/anicca/anicca-1.9.2-revert
git add apps/api/src/jobs/__tests__/affirmationSenderPayload.test.js
git commit -m "test(api): ② verify affirmation sender source (AffirmationApnsSenderJob context + quoteId) — layer 2/6 [Test #4]"
```

---

## Task 3 (STAGE 4c): ① PushTokenService.register fix

**Files:** Modify `AppDelegate.swift`, `PushTokenService.swift`; Create `aniccaiosTests/RegisterPathTests.swift`

- [ ] **Step 3.1: RED — RegisterPathTests #1 #2**

Create `aniccaios/aniccaiosTests/RegisterPathTests.swift`:

```swift
import XCTest
@testable import aniccaios

@MainActor
final class RegisterPathTests: XCTestCase {

    // Test #1: 無条件 register が呼ばれる (UIApplication protocol mock)
    func test_registerForRemoteNotifications_isCalledUnconditionally() {
        let mock = MockRemoteRegistrar()
        let sut = RemoteRegistrationCoordinator(registrar: mock)
        sut.registerNow()
        XCTAssertTrue(mock.didRegister, "registerForRemoteNotifications must be called unconditionally")
    }

    // Test #2: 3 retry → 全失敗で pending flag set
    func test_pushTokenRegister_retriesThreeTimesThenSetsFlag() async {
        UserDefaults.standard.removeObject(forKey: "com.anicca.pushTokenRegistrationPending")
        let session = StubURLSession(failuresThenSuccess: Int.max)  // 常に失敗
        let svc = PushTokenService(session: session)
        await svc.register(deviceToken: Data(repeating: 0x01, count: 32))
        XCTAssertEqual(session.callCount, 3, "must retry exactly 3 times")
        XCTAssertTrue(UserDefaults.standard.bool(forKey: "com.anicca.pushTokenRegistrationPending"))
    }
}

// Test seam: register 呼出を抽象化
protocol RemoteRegistrar: AnyObject { func registerForRemoteNotifications() }
final class MockRemoteRegistrar: RemoteRegistrar {
    var didRegister = false
    func registerForRemoteNotifications() { didRegister = true }
}
final class RemoteRegistrationCoordinator {
    private let registrar: RemoteRegistrar
    init(registrar: RemoteRegistrar) { self.registrar = registrar }
    func registerNow() { registrar.registerForRemoteNotifications() }
}

// URLSession stub (TestURLProtocol パターン踏襲)
final class StubURLSession: URLSessionProtocol {
    let failuresThenSuccess: Int
    var callCount = 0
    init(failuresThenSuccess: Int) { self.failuresThenSuccess = failuresThenSuccess }
    func data(for request: URLRequest) async throws -> (Data, URLResponse) {
        callCount += 1
        if callCount > failuresThenSuccess {
            let ok = HTTPURLResponse(url: request.url!, statusCode: 200, httpVersion: nil, headerFields: nil)!
            return (Data("{}".utf8), ok)
        }
        throw URLError(.networkConnectionLost)
    }
}
```

- [ ] **Step 3.2: Run → FAIL**

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
UDID=$(xcrun simctl list devices available | grep "iPhone 17" | head -1 | sed -E 's/.*\(([A-F0-9-]{36})\).*/\1/')
xcodebuild test -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" -only-testing:aniccaiosTests/RegisterPathTests 2>&1 | tail -10
```

Expected: FAIL (URLSessionProtocol / PushTokenService(session:) 未定義)

- [ ] **Step 3.3: GREEN — PushTokenService に 3 retry + DI**

`PushTokenService.swift` の `register(deviceToken:)` を以下に置換 (既存ロジックは postOnce に分離):

```swift
protocol URLSessionProtocol { func data(for request: URLRequest) async throws -> (Data, URLResponse) }
extension URLSession: URLSessionProtocol {}

@MainActor
final class PushTokenService {
    static let shared = PushTokenService()
    private let session: URLSessionProtocol
    private let defaults = UserDefaults.standard
    private let registeredKey = "com.anicca.apnsTokenRegistered"
    private let pendingKey = "com.anicca.pushTokenRegistrationPending"
    private let logger = Logger(subsystem: "com.anicca.ios", category: "PushTokenService")

    init(session: URLSessionProtocol = NetworkSessionManager.shared.session) { self.session = session }

    var isRegistered: Bool { defaults.bool(forKey: registeredKey) }
    func markUnregistered() { defaults.set(false, forKey: registeredKey) }

    func register(deviceToken: Data) async {
        let hex = deviceToken.map { String(format: "%02x", $0) }.joined()
        let delays: [UInt64] = [0, 1_000_000_000, 2_000_000_000]
        for (attempt, delay) in delays.enumerated() {
            if delay > 0 { try? await Task.sleep(nanoseconds: delay) }
            if await postOnce(hex: hex, attempt: attempt + 1) {
                defaults.set(false, forKey: pendingKey)
                return
            }
        }
        defaults.set(true, forKey: pendingKey)
        logger.error("Push token register: all 3 attempts failed, pending flag set")
        await restoreLocalProblemNotifications()
    }

    private func postOnce(hex: String, attempt: Int) async -> Bool {
        var request = URLRequest(url: AppConfig.pushTokenRegisterURL)
        request.httpMethod = "POST"
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        request.setValue(AppState.shared.resolveDeviceId(), forHTTPHeaderField: "device-id")
        request.setValue(TimeZone.current.identifier, forHTTPHeaderField: "x-timezone")
        request.setValue(AppState.shared.effectiveLanguage.rawValue, forHTTPHeaderField: "x-lang")
        request.httpBody = try? JSONSerialization.data(withJSONObject: ["token": hex, "platform": "ios"])
        do {
            let (data, response) = try await session.data(for: request)
            guard let http = response as? HTTPURLResponse, (200..<300).contains(http.statusCode) else { return false }
            let json = (try? JSONSerialization.jsonObject(with: data)) as? [String: Any]
            if (json?["remoteProblemNudgesEnabled"] as? Bool) == true {
                defaults.set(true, forKey: registeredKey)
                await ProblemNotificationScheduler.shared.cancelAllNotifications()
                let freeIds = (0..<3).map { "free_nudge_\($0)" }
                UNUserNotificationCenter.current().removePendingNotificationRequests(withIdentifiers: freeIds)
            } else {
                markUnregistered()
                let problems = AppState.shared.userProfile.struggles
                if !problems.isEmpty { await ProblemNotificationScheduler.shared.scheduleNotifications(for: problems) }
            }
            return true
        } catch { return false }
    }

    private func restoreLocalProblemNotifications() async {
        markUnregistered()
        let problems = AppState.shared.userProfile.struggles
        if !problems.isEmpty { await ProblemNotificationScheduler.shared.scheduleNotifications(for: problems) }
    }
}
```

(注: `private init()` を `init(session:)` に変更。 既存呼出 `PushTokenService.shared` は不変)

- [ ] **Step 3.4: GREEN — AppDelegate 無条件 register**

`AppDelegate.swift` の Task ブロック (L65-72) と `applicationDidBecomeActive` (L76-78) と `registerForRemoteIfAuthorized` (L84-89) を置換:

```swift
        Task {
            // ① 1.9.3 fix: unconditional register (旧 if-authorized gate 除去、 土曜から通知停止の真因)
            await registerForRemoteNotifications()
            await SubscriptionManager.shared.refreshOfferings()
            await AuthHealthCheck.shared.warmBackend()
            if UserDefaults.standard.bool(forKey: "com.anicca.pushTokenRegistrationPending") {
                await registerForRemoteNotifications()
            }
        }
        return true
    }

    func applicationDidBecomeActive(_ application: UIApplication) {
        Task { await registerForRemoteNotifications() }
    }

    private func registerForRemoteNotifications() async {
        await MainActor.run { UIApplication.shared.registerForRemoteNotifications() }
    }
```

- [ ] **Step 3.5: Run → PASS**

```bash
xcodebuild test -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" -only-testing:aniccaiosTests/RegisterPathTests 2>&1 | tail -6
```

Expected: `** TEST SUCCEEDED **`

- [ ] **Step 3.6: Build 全体 + Commit**

```bash
xcodebuild build -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" -configuration Debug 2>&1 | grep -E "BUILD SUC|error:" | head -3
cd /Users/anicca/anicca-1.9.2-revert
git add aniccaios/aniccaios/AppDelegate.swift aniccaios/aniccaios/Services/PushTokenService.swift aniccaios/aniccaiosTests/RegisterPathTests.swift
git commit -m "fix(ios): ① register repair — unconditional register + 3-retry + pending flag — layer 3/6 [Test #1 #2]"
```

- [ ] **Step 3.7: Dais Xcode Run verify**

Dais が Xcode で Cmd+R Run → 通知許可 → DB push_tokens に新 register 行が現れるか確認。 OK で次 layer。

---

## Task 4 (STAGE 4d): ④ cold-start race fix (★ pull-only)

**Files:** Modify `AppState.swift`, `AppDelegate.swift`, `FeedRootView.swift` (additive only); Create `ColdStartRaceTests.swift`, `Maestro/v1.9.3/01-cold-launch.yaml`

- [ ] **Step 4.1: RED — ColdStartRaceTests #3**

Create `aniccaios/aniccaiosTests/ColdStartRaceTests.swift`:

```swift
import XCTest
@testable import aniccaios

@MainActor
final class ColdStartRaceTests: XCTestCase {

    // Test #3: consumePendingQuoteId は atomic get-and-clear
    func test_consumePendingQuoteId_returnsThenClears() {
        AppState.shared.pendingQuoteId = "q042"
        let first = AppState.shared.consumePendingQuoteId()
        XCTAssertEqual(first, "q042", "first call returns the queued id")
        let second = AppState.shared.consumePendingQuoteId()
        XCTAssertNil(second, "second call returns nil (cleared)")
    }
}
```

- [ ] **Step 4.2: Run → FAIL**

```bash
xcodebuild test ... -only-testing:aniccaiosTests/ColdStartRaceTests 2>&1 | tail -6
```

Expected: FAIL (pendingQuoteId / consumePendingQuoteId 未定義)

- [ ] **Step 4.3: GREEN — AppState に pull-only プロパティ (★ @Published 禁止)**

`AppState.swift` の `pendingNudgeCard` 宣言の後 (L37 付近) に追加:

```swift
    // ④ 1.9.3 cold-start race (pull-only — ★ @Published にしない、 FeedRootView は購読せず pull する)
    var pendingQuoteId: String? = nil

    /// atomic get-and-clear。 FeedRootView.onAppear が一度だけ pull する。
    func consumePendingQuoteId() -> String? {
        defer { pendingQuoteId = nil }
        return pendingQuoteId
    }
```

- [ ] **Step 4.4: GREEN — AppDelegate.didReceive で pendingQuoteId set + launch arg**

`AppDelegate.swift` の `didReceive` (L106-112) の post の後に追加:

```swift
        if let quoteId = userInfo["quoteId"] as? String, !quoteId.isEmpty {
            NotificationCenter.default.post(name: .aniccaScrollToQuote, object: nil, userInfo: ["quoteId": quoteId])
            // ④ cold-launch queue (warm path = 上の post、 cold = AppState pull)
            Task { @MainActor in AppState.shared.pendingQuoteId = quoteId }
        }
```

`didFinishLaunching` の冒頭 (resetOnLaunch チェックの後) に追加:

```swift
        #if DEBUG
        let uiArgs = ProcessInfo.processInfo.arguments
        let uiEnv = ProcessInfo.processInfo.environment
        if let idx = uiArgs.firstIndex(of: "-pendingQuoteIdOnLaunch"), idx + 1 < uiArgs.count {
            AppState.shared.pendingQuoteId = uiArgs[idx + 1]
        } else if let qid = uiEnv["PENDING_QUOTE_ID_ON_LAUNCH"] {
            AppState.shared.pendingQuoteId = qid
        }
        #endif
```

- [ ] **Step 4.5: GREEN — FeedRootView.onAppear に consume 行追加 (★ pull-only、 削除行0)**

`FeedRootView.swift` の `.onAppear` (L50-54) を以下に置換 (既存 quotes 初期化は維持、 末尾に consume 追加):

```swift
        .onAppear {
            if quotes.isEmpty {
                quotes = QuoteProvider.shared.all()
            }
            // ④ cold-launch: AppState.shared を直接 pull (★ @ObservedObject 追加せず、 再描画グラフ不変)
            if let qid = AppState.shared.consumePendingQuoteId(),
               let idx = quotes.firstIndex(where: { $0.id == qid }) {
                withAnimation { currentIndex = idx }
            }
        }
```

- [ ] **Step 4.6: AC-ZeroTouch 3 条 機械判定**

```bash
cd /Users/anicca/anicca-1.9.2-revert
# (a) 削除行数 = 0
DEL=$(git diff release/1.9.2-revert -- aniccaios/aniccaios/Views/Feed/FeedRootView.swift --numstat | awk '{print $2}')
echo "deleted lines: $DEL (MUST be 0)"
# (b) 追加行数 <= 5
ADD=$(git diff release/1.9.2-revert -- aniccaios/aniccaios/Views/Feed/FeedRootView.swift --numstat | awk '{print $1}')
echo "added lines: $ADD (MUST be <= 5)"
# (c) @ObservedObject / @EnvironmentObject = 0
grep -c '@ObservedObject\|@EnvironmentObject' aniccaios/aniccaios/Views/Feed/FeedRootView.swift
echo "(MUST be 0)"
```

Expected: deleted=0, added<=5, grep=0

- [ ] **Step 4.7: Run #3 → PASS**

```bash
xcodebuild test ... -only-testing:aniccaiosTests/ColdStartRaceTests 2>&1 | tail -6
```

Expected: `** TEST SUCCEEDED **`

- [ ] **Step 4.8: Maestro #8 — cold-launch**

Create `aniccaios/Maestro/v1.9.3/01-cold-launch.yaml`:

```yaml
appId: ai.anicca.app.ios
name: v1.9.3 Cold-launch pendingQuoteId
tags: [v1.9.3, cold-start]
---
- launchApp:
    arguments:
      PENDING_QUOTE_ID_ON_LAUNCH: "q042"
      UITESTING: "true"
- tapOn:
    text: "今はしない|Cancel|Not Now|キャンセル"
    optional: true
- extendedWaitUntil:
    visible:
      id: "feed-settings"
    timeout: 30000
    optional: true
- extendedWaitUntil:
    visible:
      id: "onboarding-welcome-cta"
    timeout: 5000
    optional: true
- takeScreenshot: "v193-01-cold-launch"
```

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
xcodebuild build -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" -configuration Debug 2>&1 | grep BUILD
APP=$(find ~/Library/Developer/Xcode/DerivedData -name "aniccaios.app" -path "*Debug-iphonesimulator*" -not -path "*Index*" | head -1)
xcrun simctl install $UDID "$APP"
maestro test Maestro/v1.9.3/01-cold-launch.yaml 2>&1 | tail -5
```

Expected: Passed

- [ ] **Step 4.9: Commit**

```bash
cd /Users/anicca/anicca-1.9.2-revert
git add aniccaios/aniccaios/AppState.swift aniccaios/aniccaios/AppDelegate.swift aniccaios/aniccaios/Views/Feed/FeedRootView.swift aniccaios/aniccaiosTests/ColdStartRaceTests.swift aniccaios/Maestro/v1.9.3/01-cold-launch.yaml
git commit -m "fix(ios): ④ cold-start race (pull-only AppState.pendingQuoteId, ZERO-TOUCH del=0) — layer 4/6 [Test #3 #8]"
```

- [ ] **Step 4.10: Dais Xcode Run verify** (通知タップ→同 quote scroll)

---

## Task 5 (STAGE 4e): ⑦ feedback (Netlify function + SettingsSheet)

**Files:** Create `feedback.js`, `feedback.test.js`, modify `landing/package.json`, `SettingsSheet.swift`, `Localizable.strings`; Create `Maestro/v1.9.3/03-feedback.yaml`

- [ ] **Step 5.1: vitest を landing に追加**

```bash
cd /Users/anicca/anicca-1.9.2-revert/apps/landing
npm install -D vitest 2>&1 | tail -3
# package.json scripts に "test": "vitest run" を追加
node -e "const p=require('./package.json'); p.scripts.test='vitest run'; require('fs').writeFileSync('./package.json', JSON.stringify(p,null,2))"
```

- [ ] **Step 5.2: RED — feedback.test.js #6 #7**

Create `apps/landing/netlify/functions/__tests__/feedback.test.js`:

```javascript
import { describe, it, expect, vi, beforeEach } from 'vitest';

let resendCalls;
beforeEach(() => {
  resendCalls = [];
  global.fetch = vi.fn(async (url, opts) => {
    resendCalls.push({ url, body: JSON.parse(opts.body) });
    return { ok: true, text: async () => '{}' };
  });
  process.env.RESEND_API_KEY = 'test-key';
});

import { handler } from '../feedback.js';

describe('⑦ feedback.js Netlify function', () => {
  it('Test #6: happy → Resend send to user@example.com', async () => {
    const res = await handler({
      httpMethod: 'POST',
      body: JSON.stringify({ text: 'great app', locale: 'ja', appVersion: '1.9.3' })
    });
    expect(res.statusCode).toBe(200);
    expect(JSON.parse(res.body)).toEqual({ ok: true });
    const resend = resendCalls.find(c => c.url.includes('api.resend.com/emails'));
    expect(resend.body.to).toBe('user@example.com');
  });

  it('Test #7: text > 2000 → 413', async () => {
    const res = await handler({
      httpMethod: 'POST',
      body: JSON.stringify({ text: 'a'.repeat(2001), locale: 'ja', appVersion: '1.9.3' })
    });
    expect(res.statusCode).toBe(413);
  });
});
```

- [ ] **Step 5.3: Run → FAIL**

```bash
cd /Users/anicca/anicca-1.9.2-revert/apps/landing
npm test 2>&1 | tail -8
```

Expected: FAIL (feedback.js 不在)

- [ ] **Step 5.4: GREEN — feedback.js (lead-magnet パターン)**

Create `apps/landing/netlify/functions/feedback.js`:

```javascript
// In-app feedback → Resend email to Dais. POST { text, locale, appVersion }
export const handler = async (event) => {
  if (event.httpMethod !== 'POST') return { statusCode: 405, body: 'method not allowed' };
  let body;
  try { body = JSON.parse(event.body || '{}'); }
  catch { return { statusCode: 400, body: 'bad json' }; }

  const text = String(body.text || '');
  const locale = String(body.locale || 'en');
  const appVersion = String(body.appVersion || 'unknown');

  if (text.length < 5) return { statusCode: 400, body: JSON.stringify({ error: 'text too short' }) };
  if (text.length > 2000) return { statusCode: 413, body: JSON.stringify({ error: 'text too long' }) };

  const RESEND_API_KEY = process.env.RESEND_API_KEY;
  if (RESEND_API_KEY) {
    const r = await fetch('https://api.resend.com/emails', {
      method: 'POST',
      headers: { Authorization: `Bearer ${RESEND_API_KEY}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({
        from: 'Anicca Feedback <onboarding@resend.dev>',
        to: 'user@example.com',
        subject: `[Anicca Feedback] ${locale} ${new Date().toISOString()}`,
        text: `App version: ${appVersion}\nLocale: ${locale}\n\n---\n\n${text}`,
      }),
    });
    if (!r.ok) {
      const txt = await r.text();
      return { statusCode: 502, body: JSON.stringify({ error: `resend: ${txt}` }) };
    }
  }
  return { statusCode: 200, body: JSON.stringify({ ok: true }) };
};
```

- [ ] **Step 5.5: Run → PASS**

```bash
cd /Users/anicca/anicca-1.9.2-revert/apps/landing
npm test 2>&1 | tail -6
```

Expected: 2 passed

- [ ] **Step 5.6: GREEN — SettingsSheet に FeedbackSection (inline、 .pbxproj 回避)**

`SettingsSheet.swift` 末尾の `}` の前 (NotificationsSettingsView の前) に追加。 SettingsSheet body の Section{SignOut} (L62-68) は **Task 6 で削除** するので、 ここでは FeedbackSection struct を追加し、 body には Task 6 でまとめて挿入する。 まず struct を file 末尾に追加:

```swift
// MARK: - ⑦ 1.9.3 Feedback section

struct FeedbackSection: View {
    var body: some View {
        Section {
            NavigationLink { FeedbackFormView() } label: {
                Label(String(localized: "feedback_link_label"), systemImage: "envelope")
            }
            .accessibilityIdentifier("feedback-link")
        } header: {
            Text(String(localized: "feedback_section_title"))
        }
        .accessibilityIdentifier("feedback-section")
    }
}

struct FeedbackFormView: View {
    @State private var text: String = ""
    @State private var sent = false
    @State private var errorMsg: String?

    var body: some View {
        Form {
            Section {
                TextEditor(text: $text).frame(minHeight: 180)
                    .accessibilityIdentifier("feedback-textarea")
                Text("\(text.count) / 2000").font(.caption2)
                    .foregroundStyle(text.count > 2000 ? .red : .secondary)
            }
            if let e = errorMsg { Section { Text(e).foregroundStyle(.red).font(.caption) } }
            Section {
                Button {
                    Task { await submit() }
                } label: {
                    Text(sent ? "✓ Sent" : String(localized: "feedback_submit_button"))
                }
                .accessibilityIdentifier("feedback-submit-button")
                .disabled(sent || text.count < 5 || text.count > 2000)
            }
        }
        .navigationTitle(String(localized: "feedback_section_title"))
        .navigationBarTitleDisplayMode(.inline)
    }

    @MainActor private func submit() async {
        errorMsg = nil
        guard let url = URL(string: "https://aniccaai.com/.netlify/functions/feedback") else { return }
        var req = URLRequest(url: url)
        req.httpMethod = "POST"
        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
        req.httpBody = try? JSONSerialization.data(withJSONObject: [
            "text": text,
            "locale": AppState.shared.effectiveLanguage.rawValue,
            "appVersion": AppConfig.appVersion
        ])
        do {
            let (_, resp) = try await URLSession.shared.data(for: req)
            guard let http = resp as? HTTPURLResponse, (200..<300).contains(http.statusCode) else {
                errorMsg = "Failed. Please try again."; return
            }
            sent = true; text = ""
        } catch { errorMsg = error.localizedDescription }
    }
}
```

(注: body への `FeedbackSection()` 挿入は Task 6 で NewsletterSection と一緒に行う)

- [ ] **Step 5.7: Localizable keys (en + ja + 4 fallback)**

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios/aniccaios/Resources
cat >> en.lproj/Localizable.strings <<'EOF'

/* v1.9.3 feedback */
"feedback_section_title" = "Send Feedback";
"feedback_link_label" = "Tell us what to improve";
"feedback_submit_button" = "Send";
EOF
cat >> ja.lproj/Localizable.strings <<'EOF'

/* v1.9.3 feedback */
"feedback_section_title" = "ご意見・改善要望";
"feedback_link_label" = "改善してほしいこと";
"feedback_submit_button" = "送信";
EOF
for lang in es fr de pt-BR; do tail -5 en.lproj/Localizable.strings >> "${lang}.lproj/Localizable.strings"; done
```

- [ ] **Step 5.8: Maestro #10 — feedback (deferred to Task 6 build)**

Create `aniccaios/Maestro/v1.9.3/03-feedback.yaml`:

```yaml
appId: ai.anicca.app.ios
name: v1.9.3 Feedback send
tags: [v1.9.3, feedback]
---
- launchApp:
    arguments: { UITESTING: "true" }
- tapOn:
    text: "今はしない|Cancel|Not Now|キャンセル"
    optional: true
- tapOn:
    id: "feed-settings"
- tapOn:
    id: "feedback-link"
    optional: true
- takeScreenshot: "v193-03-feedback"
```

- [ ] **Step 5.9: Commit (Netlify deploy は Task 6 後にまとめて)**

```bash
cd /Users/anicca/anicca-1.9.2-revert
git add apps/landing/netlify/functions/feedback.js apps/landing/netlify/functions/__tests__/feedback.test.js apps/landing/package.json apps/landing/package-lock.json aniccaios/aniccaios/Views/Feed/SettingsSheet.swift aniccaios/aniccaios/Resources/ aniccaios/Maestro/v1.9.3/03-feedback.yaml
git commit -m "feat: ⑦ feedback Netlify function + SettingsSheet FeedbackSection — layer 5/6 [Test #6 #7 #10]"
```

---

## Task 6 (STAGE 4f): ⑥ newsletter (lead-magnet 接続 + サインアウト削除)

**Files:** Modify `AppState.swift`, `SettingsSheet.swift`, `Localizable.strings`; Create `NewsletterLangTests.swift`, `Maestro/v1.9.3/02-newsletter.yaml`

- [ ] **Step 6.1: RED — NewsletterLangTests #5.5**

Create `aniccaios/aniccaiosTests/NewsletterLangTests.swift`:

```swift
import XCTest
@testable import aniccaios

final class NewsletterLangTests: XCTestCase {
    // Test #5.5: AppState.newsletterLang(for:) は .ja → "jp", .en → "en"
    func test_newsletterLang_jaMapsToJp() {
        XCTAssertEqual(AppState.newsletterLang(for: .ja), "jp")
        XCTAssertEqual(AppState.newsletterLang(for: .en), "en")
    }
}
```

- [ ] **Step 6.2: Run → FAIL**

```bash
xcodebuild test ... -only-testing:aniccaiosTests/NewsletterLangTests 2>&1 | tail -6
```

Expected: FAIL (newsletterLang 未定義)

- [ ] **Step 6.3: GREEN — AppState に newsletterLang 純粋関数**

`AppState.swift` の `consumePendingQuoteId()` の後に追加:

```swift
    /// ⑥ lead-magnet endpoint の lang param 変換 (★ I-1: ja は "jp"、 "ja" を送ると英語 letter になる)
    static func newsletterLang(for pref: LanguagePreference) -> String {
        pref == .ja ? "jp" : "en"
    }
```

- [ ] **Step 6.4: Run #5.5 → PASS**

```bash
xcodebuild test ... -only-testing:aniccaiosTests/NewsletterLangTests 2>&1 | tail -6
```

Expected: PASS

- [ ] **Step 6.5: GREEN — SettingsSheet に NewsletterSection struct + body 挿入 + SignOut 削除**

`SettingsSheet.swift` の file 末尾に NewsletterSection struct を追加:

```swift
// MARK: - ⑥ 1.9.3 Newsletter section (既存 Daily Anicca Letter 接続)

struct NewsletterSection: View {
    @State private var email = ""
    @State private var registered = false
    @State private var submitting = false
    @State private var errorMsg: String?

    var body: some View {
        Section {
            if registered {
                Text(String(localized: "newsletter_status_registered"))
                    .accessibilityIdentifier("newsletter-status-text")
            } else {
                TextField(String(localized: "newsletter_email_placeholder"), text: $email)
                    .textContentType(.emailAddress).autocapitalization(.none).disableAutocorrection(true)
                    .accessibilityIdentifier("newsletter-email-field")
                if let e = errorMsg { Text(e).font(.caption).foregroundStyle(.red) }
                Button {
                    Task { await submit() }
                } label: {
                    HStack { if submitting { ProgressView() }; Text(String(localized: "newsletter_submit_button")) }
                }
                .accessibilityIdentifier("newsletter-submit-button")
                .disabled(submitting || !isValidEmail(email))
            }
        } header: {
            Text(String(localized: "newsletter_section_title"))
        } footer: {
            Text(String(localized: "newsletter_section_footer")).font(.caption2).foregroundStyle(.secondary)
        }
        .accessibilityIdentifier("newsletter-section")
    }

    private func isValidEmail(_ s: String) -> Bool {
        s.range(of: #"^[^@\s]+@[^@\s]+\.[^@\s]+$"#, options: .regularExpression) != nil
    }

    @MainActor private func submit() async {
        submitting = true; defer { submitting = false }; errorMsg = nil
        guard let url = URL(string: "https://aniccaai.com/.netlify/functions/lead-magnet") else { return }
        var req = URLRequest(url: url)
        req.httpMethod = "POST"
        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
        req.httpBody = try? JSONSerialization.data(withJSONObject: [
            "email": email,
            "lang": AppState.newsletterLang(for: AppState.shared.effectiveLanguage)  // ★ ja → "jp"
        ])
        do {
            let (_, resp) = try await URLSession.shared.data(for: req)
            guard let http = resp as? HTTPURLResponse, (200..<300).contains(http.statusCode) else {
                errorMsg = "Failed. Please try again."; return
            }
            registered = true
        } catch { errorMsg = error.localizedDescription }
    }
}
```

SettingsSheet body の `Section { Button(role:.destructive){ /* Sign out */ } ... }` (L62-68) を **削除** し、 その位置に挿入:

```swift
            NewsletterSection()
            FeedbackSection()
```

(= 既存 Section{Theme/Notif/Liked} と Section{Subscription} の後、 旧 SignOut Section を NewsletterSection + FeedbackSection で置換)

- [ ] **Step 6.6: Localizable newsletter keys**

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios/aniccaios/Resources
cat >> en.lproj/Localizable.strings <<'EOF'

/* v1.9.3 newsletter */
"newsletter_section_title" = "Daily Letter";
"newsletter_section_footer" = "One short letter every morning, on impermanence.";
"newsletter_email_placeholder" = "your@email.com";
"newsletter_submit_button" = "Subscribe";
"newsletter_status_registered" = "Subscribed to the daily letter.";
EOF
cat >> ja.lproj/Localizable.strings <<'EOF'

/* v1.9.3 newsletter */
"newsletter_section_title" = "毎朝の手紙";
"newsletter_section_footer" = "毎朝ひとつ、 無常についての短い手紙が届きます。";
"newsletter_email_placeholder" = "your@email.com";
"newsletter_submit_button" = "登録する";
"newsletter_status_registered" = "毎朝の手紙を登録しました。";
EOF
for lang in es fr de pt-BR; do tail -7 en.lproj/Localizable.strings >> "${lang}.lproj/Localizable.strings"; done
```

- [ ] **Step 6.7: AC-NoSignOut verify**

```bash
cd /Users/anicca/anicca-1.9.2-revert
grep -c "settings_sign_out" aniccaios/aniccaios/Views/Feed/SettingsSheet.swift
```

Expected: `0`

- [ ] **Step 6.8: Build + Maestro #9**

Create `aniccaios/Maestro/v1.9.3/02-newsletter.yaml`:

```yaml
appId: ai.anicca.app.ios
name: v1.9.3 Newsletter signup
tags: [v1.9.3, newsletter]
---
- launchApp:
    arguments: { UITESTING: "true" }
- tapOn:
    text: "今はしない|Cancel|Not Now|キャンセル"
    optional: true
- tapOn:
    id: "feed-settings"
- assertVisible:
    id: "newsletter-section"
    optional: true
- takeScreenshot: "v193-02-newsletter"
```

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
xcodebuild build -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" -configuration Debug 2>&1 | grep -E "BUILD SUC|error:" | head -3
APP=$(find ~/Library/Developer/Xcode/DerivedData -name "aniccaios.app" -path "*Debug-iphonesimulator*" -not -path "*Index*" | head -1)
xcrun simctl install $UDID "$APP"
maestro test Maestro/v1.9.3/02-newsletter.yaml Maestro/v1.9.3/03-feedback.yaml 2>&1 | tail -5
```

- [ ] **Step 6.9: M-6 — Netlify endpoint curl verify**

```bash
# feedback.js を Netlify に deploy (aniccaai.com は手動 deploy)
cd /Users/anicca/anicca-1.9.2-revert/apps/landing
set -a; . ~/.openclaw/.env; set +a
npx netlify deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod --dir=out 2>&1 | tail -5 || echo "manual deploy needed"
# newsletter endpoint (既存 live) を curl で 200+{ok:true} 確認
curl -s -X POST https://aniccaai.com/.netlify/functions/lead-magnet \
  -H "Content-Type: application/json" \
  -d '{"email":"smoke-1.9.3@anicca.test","lang":"jp"}' -w "\nHTTP %{http_code}\n"
# feedback endpoint を curl で 200+{ok:true} 確認
curl -s -X POST https://aniccaai.com/.netlify/functions/feedback \
  -H "Content-Type: application/json" \
  -d '{"text":"smoke test 1.9.3","locale":"ja","appVersion":"1.9.3"}' -w "\nHTTP %{http_code}\n"
```

Expected: 両方 `HTTP 200` + `{"ok":true}`

- [ ] **Step 6.10: Commit**

```bash
cd /Users/anicca/anicca-1.9.2-revert
git add aniccaios/aniccaios/AppState.swift aniccaios/aniccaios/Views/Feed/SettingsSheet.swift aniccaios/aniccaios/Resources/ aniccaios/aniccaiosTests/NewsletterLangTests.swift aniccaios/Maestro/v1.9.3/02-newsletter.yaml
git commit -m "feat(ios): ⑥ newsletter (Daily Letter lead-magnet, lang ja→jp) + サインアウト削除 — layer 6/6 [Test #5.5 #9]"
```

- [ ] **Step 6.11: Dais Xcode Run verify** (newsletter 登録→Day-0 letter / feedback 送信→Gmail)

---

## Task 7 (STAGE 5): Code review

- [ ] **Step 7.1: spec compliance review (architect/code-reviewer subagent)**

全 commit (feature/anicca-1.9.3) を spec v4 に対して review。 ZERO-TOUCH 守られてるか + 12 test 全 cover + 各 layer の AC。

- [ ] **Step 7.2: receiving-code-review で反映 → ok まで反復**

---

## Task 8 (STAGE 6): 全 Maestro + regression

**Files:** Create `Maestro/v1.9.3/04-imfeed-regression.yaml`

- [ ] **Step 8.1: regression flow**

```yaml
appId: ai.anicca.app.ios
name: v1.9.3 IM-feed regression
tags: [v1.9.3, regression]
---
- launchApp:
    arguments: { UITESTING: "true" }
- tapOn:
    text: "今はしない|Cancel|Not Now|キャンセル"
    optional: true
- assertVisible:
    id: "feed-settings"
    optional: true
- swipe:
    direction: UP
    optional: true
- tapOn:
    id: "quote-like"
    optional: true
- tapOn:
    id: "feed-settings"
- assertVisible:
    id: "settings-theme"
    optional: true
- takeScreenshot: "v193-04-regression"
```

- [ ] **Step 8.2: 全 Maestro v1.9.3/ 実走**

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
maestro test Maestro/v1.9.3/ 2>&1 | tail -8
```

Expected: 4/4 Passed

- [ ] **Step 8.3: 全 XCTest + vitest**

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
xcodebuild test -project aniccaios.xcodeproj -scheme aniccaios -destination "id=$UDID" 2>&1 | grep -E "TEST SUC|TEST FAIL" | head -3
cd ../apps/api && npm test 2>&1 | tail -3
cd ../landing && npm test 2>&1 | tail -3
```

- [ ] **Step 8.4: Commit**

```bash
git add aniccaios/Maestro/v1.9.3/04-imfeed-regression.yaml
git commit -m "test(maestro): v1.9.3 IM-feed regression flow [Test #11]"
```

---

## Task 9 (STAGE 7): release build 368 + asc submit

- [ ] **Step 9.1: version bump 1.9.3 / build 368**

```bash
cd /Users/anicca/anicca-1.9.2-revert/aniccaios
sed -i.bak 's/MARKETING_VERSION = 1\.9\.2/MARKETING_VERSION = 1.9.3/g' aniccaios.xcodeproj/project.pbxproj
sed -i.bak 's/CURRENT_PROJECT_VERSION = 367/CURRENT_PROJECT_VERSION = 368/g' aniccaios.xcodeproj/project.pbxproj
rm aniccaios.xcodeproj/project.pbxproj.bak
for plist in aniccaios/Info.plist AniccaNotificationService/Info.plist AniccaWidget/Info.plist; do
  plutil -replace CFBundleShortVersionString -string "1.9.3" "$plist"
  plutil -replace CFBundleVersion -string "368" "$plist"
done
```

- [ ] **Step 9.2: fastlane build + altool upload**

```bash
source ~/.config/mobileapp-builder/.env
security unlock-keychain -p "$KEYCHAIN_PASSWORD" ~/Library/Keychains/login.keychain-db
fastlane build 2>&1 | tail -5
xcrun altool --upload-app -f fastlane/build/export/aniccaios.ipa -t ios \
  --apiKey 646Y27MJ8C --apiIssuer f53272d9-c12d-4d9d-811c-4eb658284e74 2>&1 | tail -5
```

Expected: `UPLOAD SUCCEEDED`

- [ ] **Step 9.3: wait for processing**

```bash
for i in $(seq 1 15); do
  sleep 60
  STATE=$(asc builds list --app 6755129214 --platform IOS --version "1.9.3" --output json 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('data',[{}])[0].get('attributes',{}).get('processingState','?'))" 2>&1)
  echo "build 368: $STATE"
  [ "$STATE" = "VALID" ] && break
done
```

- [ ] **Step 9.4: create 1.9.3 version + attach + submit**

```bash
asc versions create --app 6755129214 --version "1.9.3" --platform IOS --release-type AFTER_APPROVAL --copy-metadata-from "1.9.0" --output json
VID=$(asc versions list --app 6755129214 --platform IOS --version "1.9.3" --output json | python3 -c "import json,sys; print(json.load(sys.stdin)['data'][0]['id'])")
BID=$(asc builds list --app 6755129214 --platform IOS --version "1.9.3" --build-number "368" --output json | python3 -c "import json,sys; print(json.load(sys.stdin)['data'][0]['id'])")
asc review submit --app 6755129214 --version-id $VID --build $BID --confirm --output json --pretty
```

- [ ] **Step 9.5: verify WAITING_FOR_REVIEW (verification-before-completion)**

```bash
asc versions view --version-id $VID --include-build --include-submission --output json --pretty
```

Expected: `"state": "WAITING_FOR_REVIEW"` + buildVersion 368

- [ ] **Step 9.6: finishing-a-development-branch — main merge + release/1.9.3 tag**

superpowers:finishing-a-development-branch で 4 option 提示 → PR/merge → release/1.9.3 tag push。

---

## Self-Review

**Spec coverage:**
- [x] ① register → Task 3
- [x] ② sender verify → Task 2
- [x] ④ cold-start race (pull-only) → Task 4
- [x] ⑤ JA humanize → Task 1
- [x] ⑥ newsletter (lead-magnet, lang jp) + サインアウト削除 → Task 6
- [x] ⑦ feedback (Netlify) → Task 5
- [x] AC-ZeroTouch 3 条 → Task 4 Step 4.6
- [x] M-6 curl verify → Task 6 Step 6.9
- [x] Test Matrix 12 件: #1#2 (T3) #3 (T4) #4 (T2) #5 (T1) #5.5 (T6) #6#7 (T5) #8 (T4) #9 (T6) #10 (T5) #11 (T8)

**Placeholder scan:** 全 step に実コード/実コマンド。 "TODO"/"similar to" なし。

**Type consistency:**
- `AppState.newsletterLang(for:)` static → T6 で定義、 T6 SettingsSheet で使用 一致
- `AppState.consumePendingQuoteId()` → T4 で定義・使用 一致
- `PushTokenService(session:)` DI → T3 test + impl 一致
- `pendingQuoteId` plain var (@Published なし) → T4 全 step 一致
- feedback.js `handler` export → T5 test + impl 一致

**ZERO-TOUCH:** Task 4 Step 4.6 で 3 条機械判定。 FeedRootView は onAppear 末尾追加のみ (削除0)。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-04-anicca-1.9.3.md`. Two execution options:

**1. Subagent-Driven (recommended)** — fresh subagent per task + two-stage review。 ただし HARD RULE (feedback_no_parallel_implementation) で iOS 実装は順次 inline が安全。

**2. Inline Execution** — executing-plans で sequential + 各 layer 後 Dais Xcode Run checkpoint。 1.9.1 教訓で各 layer Dais verify 必須なので **こちらを推奨**。

Which approach?
