# x402 External Discovery Implementation Plan

**Goal:** The production `x402-agents` Railway origin publishes a complete OpenAPI discovery contract for all nine paid routes, passes the official AgentCash/x402scan discovery and endpoint audits, and becomes externally searchable without changing payment settlement or stopping any existing loop.

**Architecture:** Extract the paid-route catalog into one module shared by the payment middleware and generated OpenAPI document. Serve `/openapi.json` before the fail-closed paid-route guard. Each operation carries an exact input schema, a decimal USD `x-payment-info` block, a `402` response, realistic output metadata, and contact/guidance fields. Keep runtime payment configuration authoritative and add contract tests that fail on route, price, method, or schema drift.

**Primary-source basis:**

- x402scan integration spec: “OpenAPI is the canonical discovery contract. Publish your spec at /openapi.json.” It also requires each payable operation to include `x-payment-info`, `protocols: [{"x402": {}}]`, a `402` response, and an input schema.
  https://github.com/Merit-Systems/x402scan/blob/main/apps/scan/src/app/(app)/(home)/integration-spec/page.tsx
- x402scan registry source: “Endpoints missing an input schema are reported as skipped.”
  https://github.com/Merit-Systems/x402scan/blob/main/apps/scan/src/lib/discovery/register-origin.ts
- Coinbase Bazaar: sellers must register `bazaarResourceServerExtension`, attach `declareDiscoveryExtension()`, and complete a CDP-facilitated settlement; metadata should contain clear examples, documented inputs, semantic descriptions, and schema-valid extensions.
  https://docs.cdp.coinbase.com/x402/bazaar

The existing user instruction to finish the remaining earning TODOs authorizes publishing this specific already-live product origin after its deployed audit passes. No self-payment may be counted as revenue.

## Task 1: RED discovery contract tests

- [x] Assert `GET /openapi.json` is public and returns OpenAPI 3.1 JSON even when the paid router is fail-closed.
- [x] Assert all nine middleware routes appear exactly once with matching method, decimal price, `x-payment-info`, `responses.402`, operation id, input schema, and output schema/example.
- [x] Assert health and settlement feed stay public but are not misclassified as paid products.
- [x] Assert route catalog contains no secret/key material and only the approved contact email.

## Task 2: GREEN single-source catalog and OpenAPI

- [x] Add `src/lib/discovery.js` with the nine route contracts and an OpenAPI builder.
- [x] Replace the inline payment route object in `server.js` with the shared catalog.
- [x] Serve `/openapi.json` before x402 initialization and the fail-closed guard.
- [x] Preserve existing descriptions, prices, networks, payee behavior, settlement hook, and route handlers.

## Task 3: Local verification

- [x] Run focused RED/GREEN tests.
- [x] Run the complete `apps/x402-agents` suite.
- [x] Run `@agentcash/discovery discover` and direct 402 checks against a local server fixture.
- [x] Run JSON parse, syntax, diff, and changed-commit secret scans.

## Task 4: Release and production proof

- [x] Commit/push and merge via PR to `anicca-products/main`.
- [x] Confirm the manually uploaded merged source reaches a `SUCCESS` Railway deployment; record the deployment/image digest separately because this service does not populate `meta.commitHash`.
- [x] Verify `/health`, `/openapi.json`, all nine real unauthenticated `402` challenges, and `/settlements`.
- [x] Run the official discovery/check audit against the final Railway origin until clean.
- [x] Register the final origin on x402scan using the existing agent-owned wallet authentication path and verify paid search readback.
- [x] Confirm Coinbase Bazaar indexing independently.
- [x] Submit the origin to PayAPI Market's free review queue using the approved provider identity and production payee.
- [x] Record evidence without claiming external revenue until a colony-external finalized receipt reaches the ledger.
