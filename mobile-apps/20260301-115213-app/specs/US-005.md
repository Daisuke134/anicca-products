# US-005: ASC + IAP + RevenueCat (INFRASTRUCTURE FIRST)

## Skills to Read
- .claude/skills/asc-signing-setup/SKILL.md
- .claude/skills/asc-subscription-localization/SKILL.md
- .claude/skills/asc-ppp-pricing/SKILL.md
- .claude/skills/mobileapp-builder/SKILL.md (CRITICAL RULES)

## Steps
1. Privacy Policy → GitHub Pages
2. asc-signing-setup: certs + profiles
3. ASC app creation — MANUAL (2FA required):
   Send to Slack:
   ```
   🏭 US-005: ASC アプリ作成が必要です
   https://appstoreconnect.apple.com/apps → 左上「＋」→「新規App」
   プラットフォーム: ☑️ iOS
   名前: ${APP_NAME} | プライマリ言語: English (U.S.)
   バンドルID: ${BUNDLE_ID} — Xcode Managed | SKU: ${SKU}
   ユーザアクセス: アクセス制限なし
   「作成」→ URL の数字（App ID）を Slack に送ってください。
   ```
   Check progress.txt for APP_ID=. If not present → passes:false, exit.
4. Subscription group + monthly + annual IAP
5. 175-country subscription pricing (price-point IDs, NOT --tier)
6. RevenueCat: products + offering + packages
7. SPM: add RevenueCat + RevenueCatUI
8. PrivacyInfo.xcprivacy (Apple WWDC23: developer.apple.com/videos/play/wwdc2023/10060/)
9. ITSAppUsesNonExemptEncryption=NO (Apple: developer.apple.com/documentation/bundleresources/information-property-list/itsappusesnonexemptencryption)
10. 全ロケール表示名設定（asc-subscription-localization SKILL.md に従う）
11. availableInNewTerritories=true で175カ国配信
12. サブスク価格設定（asc-ppp-pricing SKILL.md に従う）:
    - Monthly: asc subscriptions price-points list --subscription-id <ID> --territory USA --paginate → $4.99 の price point ID → asc subscriptions prices add
    - Annual: asc subscriptions price-points list --subscription-id <ID> --territory USA --paginate → $29.99 の price point ID → asc subscriptions prices add
13. サブスクグループのローカライゼーション設定

## Verification (ALL must PASS — evidence required)
- [ ] `asc subscriptions list` → state ≠ MISSING_METADATA (Monthly + Annual)
- [ ] `asc subscriptions prices list --id <MONTHLY>` → count > 0
- [ ] `asc subscriptions prices list --id <ANNUAL>` → count > 0
- [ ] `grep 'import RevenueCat' *.swift` → matches > 0
- [ ] `asc validate subscriptions` → blocking=0

⛔ MISSING_METADATA で passes:true にするな（validate.sh が自動リセット）
