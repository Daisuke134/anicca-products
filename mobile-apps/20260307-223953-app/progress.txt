## Codebase Patterns

- iTunes Search API: `curl -s "https://itunes.apple.com/search?term=KEYWORD&media=software&entity=software&limit=10" | jq '.results[]'`
- iTunes Reviews API: `curl -s "https://itunes.apple.com/us/rss/customerreviews/page=1/id=APP_ID/sortby=mostrecent/json" | jq '.feed.entry[]'`
- DuckDuckGo fallback when Google CAPTCHA blocks: `firecrawl scrape "https://html.duckduckgo.com/html/?q=QUERY" markdown`
- Spec docs contain prohibition context (e.g. "RevenueCatUI prohibited") — grep matches are NOT violations, verify context
- Service classes (reference types) are exempt from struct immutability rule — `private(set) var` mutation is standard pattern
- Breathing 4-7-8 cycle = 19s. Total rounds = floor(totalDuration / 19)
- Fastlane path: `FrostDipios/fastlane/Fastfile` (not `FrostDipios/Fastfile`)
- Session auto-saved as draft on timer completion (F-003). Summary shows Done/Delete/Add Notes
- Free users on Progress tab: blurred preview + "Unlock Progress" button → PaywallView sheet
- Apify TikTok scraper requires `mcpc mcp.apify.com login` before use — auth token expires frequently
- Exclusion categories must be dynamically built from `ls mobile-apps/` + reading each app's `spec/01-trend.md`
- ASC subscription availability MUST be set before pricing (reverse = 500 error)
- Equalization API excludes USA — add USA price separately after CSV import
- `asc subscriptions list --group GROUP_ID` (not `--app APP_ID`)
- Bash array for locale loop: `LOCALES=(en-US ja ...)` + `for L in "${LOCALES[@]}"`
- SubscriptionService MUST be singleton (`static let shared`) — SwiftUI re-creates view structs, so `let service = Service()` loses state
- Timer.scheduledTimer defaults to RunLoop .default (pauses on scroll) — use `RunLoop.current.add(timer, forMode: .common)` for user-facing timers
- assertionFailure in @main App.init() crashes test runner — never use in app entry point
- xcodegen xcconfig: use `configFiles:` at target level, NOT `XCCONFIG_FILE` in settings.configs
- Purchases.shared crashes if not configured — guard all calls with `isConfigured` flag on SubscriptionService
- Maestro: NavigationStack/TabView container a11y IDs invisible — use child element IDs for assertions
- Maestro: Tab bar items need text: selector (a11y ID on content view invisible when tab not selected)
- Maestro: uiPreviewMode auto-completes RC purchases — make "Test valid purchase" tap optional
- Maestro path: `$HOME/.maestro/bin/maestro` (not in /opt/homebrew/bin)
- Maestro clearState does NOT reset @AppStorage on iOS 26 — use `xcrun simctl uninstall` + `defaults delete $BUNDLE_ID`
- axe tap --id broken on iOS 26 (can't find SwiftUI accessibilityIdentifier) — use Maestro for navigation instead
- Screenshots: 6.9" only (IPHONE_69 → ASC maps to APP_IPHONE_67) — Apple auto-scales to all smaller sizes
- @AppStorage("selected_tab") on MainTabView enables external tab selection via `defaults write` for screenshot automation

---

## 2026-03-07 - US-001: Trend Research + Idea Selection
- Selected: FrostDip (Cold plunge & ice bath tracker), score 8.5/10
- 25+ raw ideas generated across 5 lenses
- iTunes API confirmed HUGE market gap: top standalone tracker Brisk has only 58 reviews
- Cold plunge market: $512.9M in 2026 (Persistence Market Research)
- 12 exclusion categories dynamically generated from existing apps
- Top 5: FrostDip (8.5), ContrastTimer (7.7), SupplTrack (7.5), FlexTrack (6.9), WalkStory (6.8)
- Files: spec/01-trend.md
- **Learnings for future iterations:**
  - Google CAPTCHA blocks Firecrawl scrapes — use DuckDuckGo HTML endpoint as fallback
  - Apify TikTok scraper auth expires — need `mcpc mcp.apify.com login` before each session
  - Cold plunge tracker category has extremely low competition — most apps <100 reviews
  - Plunge app (1,518 reviews) is hardware-tied with terrible UX — standalone opportunity is massive
---

## 2026-03-07 - US-002: Product Planning
- Created product-plan.md with all 7 required sections (Target User, Problem, Solution, Monetization, MVP Scope, App Identity, Risk Assessment)
- 15 external sources cited (RevenueCat SOSA, Persistence Market Research, World Metrics, Harvard Health, PLOS ONE, iTunes API, Jake Mor, etc.)
- Pricing: $6.99/mo (90% of H&F median), $29.99/yr (64% apparent discount)
- MoSCoW prioritization: 13 Must Have features, 12 Won't Have with reasons
- Bundle ID: com.aniccafactory.frostdip, App Title: "Cold Plunge Timer - FrostDip"
- iTunes name check: 0 exact matches for "FrostDip" (verified 2026-03-07)
- Competitive analysis: 6 competitors analyzed (Plunge, Brisk, Shiver, GoPolar, IceBuddy, Wim Hof)
- Files: product-plan.md
- **Learnings for future iterations:**
  - iTunes Search API requires `+` for spaces in URL-encoded queries (not `%20`)
  - Cold plunge app market is extremely low competition — top standalone has only 58 reviews
  - RevenueCat SOSA H&F median ($7.73/mo, $29.65/yr) is the pricing anchor for all health apps
  - PROHIBITED features list from us-002 reference is critical for MVP scoping — saves implementation time
---

## 2026-03-07 - US-003: Market Research
- Created competitive-analysis.md (6 sections): 8 competitors profiled (5 direct: Brisk, Shiver, IceBuddy, ICY, ColdTrack; 3 indirect: Plunge, GoPolar, Wim Hof), feature comparison matrix, pricing analysis with positioning map, SWOT for top 3 + FrostDip, feature gap analysis from 1-2 star reviews
- Created market-research.md (5 sections): TAM/SAM/SOM (bottom-up: 228K potential users, SAM 75K iOS US+JP, SOM Y3 ~6K users / $28K ARR), problem size with 10 statistics, growth analysis (CAGR 4.9-5.4%), POEM score 22/25 (Strong Opportunity)
- 24 unique sources cited across both documents (8 in competitive-analysis, 16 in market-research)
- Files: competitive-analysis.md, market-research.md
- **Learnings for future iterations:**
  - Bottom-up TAM is far more credible than top-down for indie apps — 112x gap between approaches is normal
  - Cold plunge app market has NO dominant player: top standalone tracker (Brisk) has only 58 reviews
  - GoPolar users complain about sync failures and overpriced subscription — opportunity for reliable, fairly-priced alternative
  - Brisk's timer only supports minute increments — second-level precision is a clear differentiator
  - Negative signal: low review counts across ALL competitors could mean low demand, not just poor products
---

## 2026-03-07 - US-004a: Core Spec (PRD, ARCHITECTURE, IMPLEMENTATION_GUIDE)
- Created docs/PRD.md (302 lines): 14 sections, app_name/bundle_id/prices, 15 Feature IDs (F-001 to F-015), paywall design requirements, en-US + ja App Store metadata
- Created docs/ARCHITECTURE.md (408 lines): 12 sections, MVVM architecture, ASCII system diagram, directory structure, Protocol-based DI for all 4 services, SwiftData models, UserDefaults key table, PrivacyInfo.xcprivacy template
- Created docs/IMPLEMENTATION_GUIDE.md (655 lines): 8 sections, 9 implementation phases with Feature ID mapping, xcconfig for RC_API_KEY (no hardcoding), full code samples for SubscriptionService/HealthKitService/NotificationService/PaywallView, Fastfile config, Greenlight check commands
- Total: 1,365 lines across 3 documents
- Gate 1 (PRD quality): 6/6 PASS
- Rule compliance: Rule 17/20/20b/21 all mentions are prohibition context only — zero actual usage
- Files: docs/PRD.md, docs/ARCHITECTURE.md, docs/IMPLEMENTATION_GUIDE.md
- **Learnings for future iterations:**
  - Gate check grep for Rule violations will match prohibition descriptions in spec docs — verify context, not just presence
  - PRD Feature IDs (F-001 to F-015) must propagate to IMPLEMENTATION_GUIDE Phase Breakdown for traceability
  - xcconfig + Info.plist for API keys prevents hardcoding and passes Greenlight checks
---

## 2026-03-07 - US-004b: UX Spec (UX_SPEC, DESIGN_SYSTEM, TEST_SPEC, RELEASE_SPEC)
- Created docs/DESIGN_SYSTEM.md (198 lines): 7 sections — 8 brand color tokens (primary, accent, cold, hot, success, warning, destructive + semantic system colors), 12 typography styles (San Francisco + monospaced timer/HR), 8pt grid spacing scale, 8 reusable components, 19 SF Symbols, 10 animations with reduced motion fallbacks, 4 haptic levels, a11y contrast requirements
- Created docs/UX_SPEC.md (327 lines): 9 sections — 2 personas (Cold Plunge Chris, Contrast Therapy Dana), IA diagram with onboarding gate + 4-tab structure, 4 navigation tabs with SF Symbols, 11 screen inventory, 5 ASCII wireframes (Timer, BreathingPrep, Paywall, History, Progress), 4-step onboarding flow (3-act structure per Mau rules), 45 accessibilityIdentifiers for Maestro E2E, gesture map, localization layout rules
- Created docs/TEST_SPEC.md (283 lines): 7 sections — test pyramid (70/20/10 split), 40 unit tests (10 model + 15 service + 10 viewmodel + 5 streak), 8 integration tests, 7 Maestro E2E flows (2 with smokeTest tag), 8 Greenlight automated checks, performance targets (launch <1.5s, memory <80MB, 60fps timer)
- Created docs/RELEASE_SPEC.md (286 lines): 11 sections — 9 pre-submission gates, en-US + ja metadata (exact copy from PRD §14), 5 screenshots per locale with capture/frame/upload commands, PrivacyInfo.xcprivacy template, build pipeline, TestFlight setup, submission commands, review notes, 22 age rating items (all NONE = 4+), hotfix protocol, version history
- Gate 2 (design integrity): 6/6 PASS — Feature IDs in ARCH (8), no RevenueCatUI (prohibition context only), no analytics (prohibition context only), Paywall in UX, 8 color tokens, all 3 files exist
- Gate 3 (cross-reference): All PASS — 15/15 Feature IDs traced PRD→IMPL, product prices match ($6.99, $29.99), RevenueCat SDK present, Rule violations are prohibition context only
- Total: 7 docs, 2,756 lines combined
- Files: docs/DESIGN_SYSTEM.md, docs/UX_SPEC.md, docs/TEST_SPEC.md, docs/RELEASE_SPEC.md
- **Learnings for future iterations:**
  - Gate check grep matches prohibition descriptions in spec docs — always verify context, not just string presence (confirmed pattern from US-004a)
  - Price comparison between PRD and RELEASE_SPEC will show reference prices (medians, market sizes) as mismatches — only product prices ($6.99, $29.99) matter
  - Onboarding 3-act structure (Problem → Experience → Paywall) from Mau is the conversion-optimized pattern for health apps
  - 45 a11y identifiers in UX_SPEC must match TEST_SPEC Maestro E2E flows exactly — single source for both
---

## 2026-03-07 - US-005a: Privacy Policy + ASC App Creation (IN PROGRESS)
- Bundle ID registered: GCVGP8K25Q (com.aniccafactory.frostdip)
- iris session confirmed active
- Privacy Policy URL: https://aniccaai.com/privacy (existing, no deployment needed)
- PrivacyInfo.xcprivacy + Info.plist: deferred to US-006 (per spec)
- Project .env dir: ~/.config/mobileapp-builder/projects/frostdip/
- HUMAN_RESOLVED: 2FA code received
- Attempt 2: iris session confirmed active, Bundle ID GCVGP8K25Q confirmed
- ASC App created successfully: APP_ID=6760225278, name="Cold Plunge Timer - FrostDip"
- APP_ID saved to ~/.config/mobileapp-builder/projects/frostdip/.env
- Files: progress.txt, prd.json (updated)
- **Learnings for future iterations:**
  - `asc apps create` requires `--two-factor-code` flag for non-interactive shells (EOF error without it)
  - iris session can be active while still requiring 2FA for app creation (session != 2FA)
  - Bundle ID must be registered before `asc apps create` or it returns 500 error
---

## 2026-03-07 - US-004-R: Spec Review (all 7 documents)
- code-quality-reviewer agent ran on all 7 spec docs (PRD, ARCHITECTURE, IMPLEMENTATION_GUIDE, UX_SPEC, DESIGN_SYSTEM, TEST_SPEC, RELEASE_SPEC)
- Initial findings: 5 CRITICAL, 9 WARNING, 6 SUGGESTION
- All 5 CRITICALs fixed:
  1. Contrast therapy data model: added hotTemperature/coldDuration/hotDuration/roundsCompleted to PlungeSession, hotTime to PlungeProtocol
  2. listenForUpdates: replaced one-shot getCustomerInfo with Purchases.shared.customerInfoStream
  3. Service-layer mutation: documented exception to immutability rule for reference-type services
  4. history_browse.yaml: added full Maestro YAML template with onboarding skip + timer + History tab
  5. HealthKit no-HR-data: added 30s timeout error case with user message
- 8 WARNINGs also fixed: @Observable standardized, breathing cycle count, session_card a11y, Fastfile path, auto-save/discard, Progress free-user UX, xcodebuild→fastlane, StreakService extraction
- Re-review confirmed CRITICAL=0
- Files changed: docs/ARCHITECTURE.md, docs/IMPLEMENTATION_GUIDE.md, docs/TEST_SPEC.md, docs/DESIGN_SYSTEM.md, docs/UX_SPEC.md
- **Learnings for future iterations:**
  - Grep-based Gate checks match prohibition context in spec docs — always verify context, not just presence (confirmed 3x)
  - Contrast therapy features need per-round data model fields from the start — adding later causes migration headaches
  - RevenueCat v5 customerInfoStream replaces callback-based getCustomerInfo for continuous listening
  - StreakService should be extracted from HistoryViewModel for testability (test spec expects standalone module)
---

## 2026-03-08 - US-005b: IAP + Pricing + RevenueCat Setup (HUMAN_RESOLVED: RC_SECRET_KEY=sk_ypterPMVEqWYDhyySqcFkKDFlAtFn
- ASC subscription group created: GROUP_ID=21966024 (FrostDip Premium)
- 3 subscriptions created: Weekly ($1.99, ID=6760225308), Monthly ($6.99, ID=6760225483), Annual ($29.99, ID=6760225430)
- 39 locales localized per subscription (group + weekly + monthly + annual = 156 total localizations)
- 175 territories availability set for all 3 subscriptions
- 175 country prices set for all 3 subscriptions (equalization API + CSV import + USA manual add)
- product-plan.md updated: Weekly $1.99 tier added (3-plan strategy per RevenueCat SOSA 2025)
- HUMAN_RESOLVED: RC_SECRET_KEY=sk_ypterPMVEqWYDhyySqcFkKDFlAtFn
- Review screenshots deferred to US-008a (app not built yet)
- SPM dependency deferred to US-006 (Xcode project not created yet)
- Files changed: product-plan.md, prd.json, progress.txt
- **Learnings for future iterations:**
  - `asc subscriptions list` requires `--group GROUP_ID`, not `--app APP_ID`
  - Shell string variable in for loop doesn't split on spaces — use bash array `LOCALES=(...)` with `"${LOCALES[@]}"`
  - availability MUST be set before pricing — reverse order causes Apple API 500
  - equalization API doesn't include USA — must add USA price separately after CSV import
  - BMU (Bermuda) can timeout on large imports — retry with single-row CSV works
  - MISSING_METADATA state is expected until review screenshots are uploaded (US-008a)
---

## 2026-03-08 - US-005b: IAP + Pricing + RevenueCat Setup (COMPLETED)
- HUMAN_RESOLVED: RC_SECRET_KEY provided
- RC Project ID: proje06970dd (auto-detected from SK key)
- RC App created: app551d163aab (FrostDip, bundle: com.aniccafactory.frostdip)
- RC Public Key: appl_bZDzQANwQsnkihybBcJlKdsybbi (saved to project .env)
- RC Entitlement: entl72c97fdb5b (premium, active)
- RC Products: 3 created (weekly=prod3c2b4f85c8, monthly=prod247359572e, annual=proddf9c98e9fc) — all attached to premium entitlement
- RC Offering: ofrngae87fc46a4 (default, is_current=true)
- RC Packages: 3 created ($rc_weekly, $rc_monthly, $rc_annual) — all attached to products
- ASC verification: 1 group, 3 subs (Weekly/Monthly/Annual), 175 prices each
- MISSING_METADATA state expected until review screenshots (US-008a)
- Files changed: prd.json, progress.txt, ~/.config/mobileapp-builder/projects/frostdip/.env
- **Learnings for future iterations:**
  - RC API v2 uses `name` (not `app_name`) and nested `app_store.bundle_id` for app creation
  - RC Public Key is auto-generated on app creation — no manual step needed
  - RC Package lookup_key must use `$rc_weekly` / `$rc_monthly` / `$rc_annual` (RC standard keys)
  - RC Package `attach_products` requires `eligibility_criteria: "all"` field
  - RC Entitlement `attach_products` uses `product_ids` (array of strings), not `products` (array of objects)
---

## 2026-03-08 - US-006a: TDD Data Layer (xcconfig, Protocol DI, Models, Services)
- Created full FrostDipios/ project structure with xcodegen (project.yml)
- xcconfig: Debug.xcconfig + Release.xcconfig with RC_API_KEY=$(RC_API_KEY), Info.plist references $(RC_API_KEY)
- 4 Service Protocols: TimerServiceProtocol, HealthKitServiceProtocol, SubscriptionServiceProtocol, NotificationServiceProtocol
- 3 Models: PlungeSession (@Model with contrast therapy fields), PlungeProtocol (@Model with defaultProtocols()), UserPreferences (UserDefaults-backed with all keys from ARCHITECTURE.md)
- AccessibilityID enum with all IDs from UX_SPEC
- 5 Services: SubscriptionService (DEBUG uiPreviewMode + DangerousSettings per us-005b pattern), TimerService (4-7-8 breathing prep), HealthKitService (anchored HR query), NotificationService (daily reminder + streak warning), StreakService (streak freeze logic)
- FrostDipApp.swift (@main with RC configure), ContentView (onboarding gate + 4-tab MainTabView)
- Theme.swift (DESIGN_SYSTEM tokens: 8 colors, 12 typography styles, spacing scale, corner radius, animations)
- PrivacyInfo.xcprivacy (CA92.1 UserDefaults)
- App Icon 1024x1024 (ice crystal on navy gradient)
- Fastfile (test, build, release lanes)
- 36 tests ALL PASS (6 PlungeSession, 5 PlungeProtocol, 12 UserPreferences, 6 Timer, 7 Streak)
- Greenlight: CRITICAL=0, WARN=1 (PLACEHOLDER check logic in code — expected)
- Gate: Mock=0, RevenueCat=4 imports, RevenueCatUI=0, AI APIs=0
- Files: FrostDipios/ (entire directory created)
- **Learnings for future iterations:**
  - xcodegen `info:` section conflicts with manual Info.plist — remove `info:` when GENERATE_INFOPLIST_FILE: NO
  - Color(uiColor: .systemBackground) required in SwiftUI — Color(.systemBackground) is ambiguous
  - iPhone 16 Pro sim may not exist — fastlane falls back to iPhone 17 Pro automatically
  - RevenueCat 5.61.0 resolved via SPM (cached) — no manual version pin needed
---

## 2026-03-08 - US-006b: TDD Onboarding + Monetization (MVVM, Onboarding, PaywallView)
- MVVM split: OnboardingViewModel (@Observable, 4-step state machine) + PaywallViewModel (@Observable, purchase/restore/load)
- 4-step onboarding flow: WelcomeView (hook + CTA) → ExperienceLevelView (3 levels + mirror text) → NotificationPermissionView (permission request + skip) → PaywallView (soft paywall)
- PaywallView: self-built SwiftUI, Purchases.shared.purchase(package:), 3-plan pricing grid (weekly $1.99 / monthly $6.99 / annual $29.99), benefit bullets x5, social proof, FAQ x3, legal links (Privacy Policy + Terms), restore purchases
- OnboardingContainerView: coordinator pattern with slide/fade transitions
- AccessibilityIDs fully aligned to UX_SPEC (45 IDs across all screens)
- ContentView updated: onboarding gate → OnboardingContainerView, MainTabView unchanged
- 18 new tests (10 OnboardingViewModel + 8 PaywallViewModel), total 54 tests ALL PASS
- Greenlight CRITICAL=0, Mock=0, RevenueCatUI=0, AI APIs=0
- Files created: ViewModels/OnboardingViewModel.swift, ViewModels/PaywallViewModel.swift, Views/Onboarding/OnboardingContainerView.swift, Views/Onboarding/WelcomeView.swift, Views/Onboarding/ExperienceLevelView.swift, Views/Onboarding/NotificationPermissionView.swift, Views/Onboarding/PaywallView.swift
- Files modified: App/ContentView.swift, Models/PlungeSession.swift (AccessibilityID update)
- Tests created: ViewModelTests/OnboardingViewModelTests.swift, ViewModelTests/PaywallViewModelTests.swift
- **Learnings for future iterations:**
  - RevenueCat Package objects can't be easily mocked in unit tests — test the guard path and restore path instead
  - @Observable requires iOS 17+ — project.yml deploymentTarget must match
  - PaywallViewModel needs packages loaded before purchase() works — guard prevents index out of bounds
  - Onboarding 3-act structure (Problem → Personalization → Commitment → Paywall) follows Mau conversion pattern
---

## 2026-03-08 - US-006c: TDD Core Screens (Timer, Settings, ProgressDashboard)
- TimerViewModel (@Observable): state machine (idle/breathing/running/paused/completed), zero-division guard (coldTime=0 blocks start), protocol selector from defaultProtocols(), formattedTime helper, session creation on completion with temperature and protocol name
- TimerView: circular timer ring with progress animation, breathing phase overlay, heart rate display, temperature stepper (0-20C, 0.5 step), action buttons (Prep/Start/Pause/Resume/Stop) per state, protocol picker (Menu), SessionSummaryView sheet on completion
- BreathingPrepView: animated circle scaling with phase (inhale=1.0, exhale=0.6), phase text, skip button
- SessionSummaryView: stats (duration, temperature, HR, protocol), notes text field, Done/Delete toolbar buttons, saves to SwiftData + records streak
- SettingsViewModel (@Observable): temperature unit toggle persisted, notifications toggle, showPaywall flag, appVersion from Bundle
- SettingsView: Preferences section (temp unit segmented picker, notification toggle), Subscription section (Upgrade → PaywallView sheet, Restore Purchases), About section (Privacy Policy link, version)
- ProgressViewModel (@Observable): stats aggregation (totalSessions, averageDuration, averageTemperature with nil handling), static DateFormatter for weekday labels, formattedDuration helper
- ProgressDashboardView: streak counters (current/longest), 30-day calendar grid (LazyVGrid, dots for sessions), Swift Charts duration trend (LineMark + PointMark), 3 stat cards (total/avg dur/avg temp), free user blurred preview with lock icon and "Unlock Progress" upgrade button → PaywallView sheet
- HistoryView: 7-day filter for free users / unlimited for premium, SessionCardView (icon, duration, temp, HR), swipe to delete, upgrade banner for gated content
- SessionDetailView: full session info (date, duration, protocol, metrics, notes)
- ContentView.MainTabView: wired to real screens (TimerView, HistoryView, ProgressDashboardView, SettingsView)
- 37 new tests (18 TimerViewModel + 8 Settings + 11 Progress), total 91 tests ALL PASS (0 failures)
- Greenlight CRITICAL=0, WARN=1 (PLACEHOLDER check logic — expected)
- Gate: Mock=0, RevenueCat=7 imports, RevenueCatUI=0, AI APIs=0
- Files created: ViewModels/TimerViewModel.swift, ViewModels/SettingsViewModel.swift, ViewModels/ProgressViewModel.swift, Views/Timer/TimerView.swift, Views/Timer/BreathingPrepView.swift, Views/Timer/SessionSummaryView.swift, Views/Settings/SettingsView.swift, Views/Progress/ProgressDashboardView.swift, Views/History/HistoryView.swift
- Files modified: App/ContentView.swift (MainTabView wired to real screens)
- Tests created: ViewModelTests/TimerViewModelTests.swift, ViewModelTests/SettingsViewModelTests.swift, ViewModelTests/ProgressViewModelTests.swift
- **Learnings for future iterations:**
  - PaywallView init uses isOnboarding/onDismiss params (not onComplete) — check existing init before calling
  - Static DateFormatter on ProgressViewModel avoids re-creation per render cycle — standard iOS optimization
  - TimerViewModel zero-division guard: check coldTime > 0 before startTimer() prevents NaN/crash
  - SubscriptionService.isPremium drives premium gating — same instance must be shared for consistent state
  - HistoryView 7-day filter: Calendar.date(byAdding: .day, value: -7) for free tier cutoff
---

## 2026-03-08 - US-006d: TDD Polish (DESIGN_SYSTEM tokens, a11y IDs, Localization)
- Theme.swift typography updated: timerDisplay weight → .bold (per DESIGN_SYSTEM spec), hrDisplay (36pt semibold monospaced) and temperatureDisplay (24pt medium monospaced) added
- Tab a11y IDs added: tab_timer, tab_history, tab_progress, tab_settings in AccessibilityID enum + applied to MainTabView in ContentView.swift
- Localizable.xcstrings created: 80+ strings with en (source) + ja translations covering all user-facing text across 11 screens
- project.yml updated: knownRegions [en, ja], defaultLocalization: en
- PrivacyInfo.xcprivacy confirmed at Resources/ (CA92.1 UserDefaults)
- ITSAppUsesNonExemptEncryption = NO confirmed in Info.plist
- 91 tests ALL PASS (0 failures), fastlane build PASS
- Greenlight CRITICAL=0, Mock=0, RevenueCatUI=0, AI APIs=0
- Files created: FrostDipios/FrostDip/Localizable.xcstrings
- Files modified: Design/Theme.swift (typography), Models/PlungeSession.swift (AccessibilityID enum), App/ContentView.swift (tab a11y IDs), project.yml (localization)
- **Learnings for future iterations:**
  - Xcode String Catalogs (.xcstrings) are JSON-based and auto-picked up by SwiftUI Text() views — no NSLocalizedString() calls needed
  - Tab accessibilityIdentifiers must be set on the tab content view (e.g. TimerView().accessibilityIdentifier()), not on the Label
  - SourceKit "Cannot find in scope" errors for cross-file references are analysis-only — actual build succeeds
  - xcodegen knownRegions + defaultLocalization in options section enables localization support
---

## 2026-03-08 - US-006-R: Code Review (006a-d full review)
- code-quality-reviewer agent ran on all 31 production Swift files (App, Models, Services, ViewModels, Views, Design)
- Initial findings: 3 CRITICAL, 7 HIGH, 6 MEDIUM, 3 LOW
- All 3 CRITICALs fixed:
  1. C-2: SubscriptionService.purchase() DEBUG path now sets isPremium=true (was silently returning true without updating state)
  2. C-3: StreakService force unwrap `calendar.date(byAdding:)!` replaced with `guard let` early return
  3. C-1: RC public API key in xcconfig — downgraded (public keys are client-side by design, shipped in every .ipa)
- All 7 HIGHs fixed:
  1. H-1/H-2/H-4: SubscriptionService.shared singleton — HistoryView, ProgressDashboardView, SettingsView, PaywallViewModel all use shared instance (was creating new instances where isPremium always false)
  2. H-3: FrostDipApp silent return on missing RC_API_KEY (assertionFailure removed — crashes test runner in DEBUG)
  3. H-5: HistoryView onDelete now uses modelContext.delete() (was no-op with `_ = modelIndex`)
  4. H-6: TimerService DRY — extracted scheduleCountdownTimer() private helper
  5. H-7: TimerService RunLoop .common — timer no longer freezes during scroll
- M-3 also fixed: HealthKitService NSLock for thread-safe samples array access
- Re-review confirmed: CRITICAL=0, Mock=0, RevenueCatUI=0, AI APIs=0, Greenlight CRITICAL=0
- 91 tests ALL PASS (0 failures)
- Files modified: Services/SubscriptionService.swift, Services/StreakService.swift, Services/TimerService.swift, Services/HealthKitService.swift, App/FrostDipApp.swift, Views/History/HistoryView.swift, Views/Progress/ProgressDashboardView.swift, Views/Settings/SettingsView.swift, ViewModels/PaywallViewModel.swift
- **Learnings for future iterations:**
  - SubscriptionService MUST be a singleton — each SwiftUI view creates a new struct on re-render, so `let service = SubscriptionService()` means isPremium is always false
  - assertionFailure in @main App.init() crashes the test runner — use silent return or print warning only
  - Timer.scheduledTimer uses RunLoop .default mode which pauses during scroll — always use RunLoop .common for user-facing timers
  - HealthKit query callbacks run on arbitrary background queues — protect shared mutable state with NSLock
  - SwiftUI .onDelete handler must call modelContext.delete() explicitly — no implicit SwiftData deletion
---

## 2026-03-08 - US-007: Testing (Maestro E2E)
- 6 Maestro E2E flows ALL PASS (6/6, 2m 20s):
  - 01-onboarding.yaml (smokeTest): Welcome → Experience → Notifications → Paywall → Maybe Later → Timer
  - 02-timer.yaml: Onboarding skip → Breathing prep → Skip → Timer run → Pause → Stop
  - 03-settings.yaml: Onboarding skip → Settings tab → Verify elements → Upgrade → Paywall → Dismiss
  - 04-payment-monthly-success.yaml (smokeTest): Onboarding → Paywall → Monthly → CTA → Timer
  - 05-payment-annual-success.yaml: Onboarding → Paywall → Annual → CTA → Timer
  - 06-payment-failure.yaml: Onboarding → Paywall → Verify all plans + CTA + Maybe Later → Dismiss → Timer
- Critical fixes during implementation:
  1. project.yml `XCCONFIG_FILE` → `configFiles` (xcodegen ignores XCCONFIG_FILE in settings.configs)
  2. SubscriptionService `isConfigured` guard on all Purchases.shared calls (crash without configure)
  3. RC_API_KEY now properly embedded in built .app (was empty string before xcconfig fix)
- Flow adjustments for uiPreviewMode:
  - Purchase dialog "Test valid purchase" made optional (uiPreviewMode auto-completes)
  - Payment failure flow tests UI elements + dismiss path (uiPreviewMode can't simulate failure)
  - Container-level a11y IDs (timer_view, settings_view) not visible to Maestro on NavigationStack — use child element IDs
  - Tab bar a11y IDs on content views not visible when tab unselected — use text: "Settings" selector
- Gate checks: 6 flows, clearState in all, takeScreenshot in all, 2 smokeTest tags, 0 point: selectors
- 91 unit tests still PASS (0 failures)
- Files created: (maestro flows already existed from previous iteration)
- Files modified: FrostDipios/project.yml (configFiles), FrostDip/Services/SubscriptionService.swift (isConfigured guard), maestro/*.yaml (selector fixes)
- **Learnings for future iterations:**
  - xcodegen `configFiles:` at target level is the correct way to reference .xcconfig files — NOT `XCCONFIG_FILE` in settings.configs
  - Purchases.shared crashes with assertionFailure if Purchases.configure() wasn't called — always guard with isConfigured flag
  - uiPreviewMode auto-completes purchases without showing RC Test Store dialog — make dialog taps optional
  - SwiftUI NavigationStack/TabView container a11y IDs are invisible to Maestro — always use child element IDs for assertions
  - Tab bar items need text: selectors when a11y IDs are on content views (invisible when tab unselected)
  - Maestro runs flows in parallel by default — each flow must be self-contained with clearState
---

## 2026-03-08 - US-008a: Screenshots (capture + upload + review screenshot)
- Maestro-driven screenshot capture on iPhone 16 Pro Max (1320x2868 = 6.9" IPHONE_69)
- en-US 4 screenshots: Welcome, Timer, History, Progress
- ja 4 screenshots: same screens with Japanese localization
- MD5 verified: en!=ja (locale correctly applied), 0 same-locale duplicates
- Old screenshots from previous iterations deleted (5 en-US APP_IPHONE_67, 3 en-US APP_IPHONE_65, 4 ja APP_IPHONE_61, 5 ja APP_IPHONE_67, 3 ja APP_IPHONE_65)
- ASC upload: en-US 4 + ja 4 = 8 total, all COMPLETE on APP_IPHONE_67 (Apple auto-scales 6.9" → 6.5"/6.3"/6.1")
- Review screenshots: Paywall (Annual selected) uploaded to Weekly (6760225308), Monthly (6760225483), Annual (6760225430)
- ContentView.selectedTab changed from @State to @AppStorage("selected_tab") for screenshot tab navigation
- 91 tests ALL PASS (0 failures)
- Files created: .asc/screenshots.json, .asc/screenshots-history.json, .asc/screenshots-progress.json, maestro/screenshot-en.yaml, maestro/screenshot-ja.yaml, maestro/screenshot-review.yaml
- Files modified: FrostDipios/FrostDip/App/ContentView.swift (@AppStorage for selectedTab)
- **Learnings for future iterations:**
  - Maestro clearState does NOT reliably reset @AppStorage/UserDefaults on iOS 26 — use manual `xcrun simctl uninstall` + `defaults delete $BUNDLE_ID` instead
  - axe tap --id does not find SwiftUI accessibilityIdentifier on iOS 26 simulators — use Maestro instead of asc screenshots run for navigation
  - Tab bar items accessible via Maestro `text:` selector (en: "History"/"Progress", ja: localized names from .xcstrings)
  - 6.9" screenshots (IPHONE_69) uploaded as APP_IPHONE_67 by Apple — this is normal Apple behavior, auto-scales to all smaller sizes
  - Paywall screenshots should NOT go on product page (Fix #1) — only for review screenshots (Step 1h)
  - @AppStorage for selectedTab enables external tab selection via `defaults write` for screenshot automation
---
