# ローカル Control UI: "Session file path must be within sessions directory" の対処

**→ 公式ドキュメントに沿った対処は次を参照してください。**

**`scripts/openclaw-vps/local-control-ui-doc-fix.md`**

---

（従来の `local-fix-session-path.js` / `open-control-ui.sh` によるワークアラウンドは、公式のやり方ではないため推奨しません。上記ドキュメントの「gateway は `openclaw gateway` のみ」「`openclaw doctor`」「VPS の state でローカルを上書きしない」で解消します。）
