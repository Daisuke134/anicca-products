# Handover — x402 B4→B5 external revenue

## SSOT

- Spec: `/Users/anicca/anicca-project/docs/superpowers/specs/2026-07-18-bounty-loop-onchain-spec.md`
- Remaining-TODO SSOT: `## STATUS（実装済み vs 残タスク）` の B4→B5 table と `## OPEN RISK / honest gap`。
- Current task: B4 external payout verify + ledger。B1/B2/B3は完了、B5はB4依存。

## Repository and runtime state

- Code worktree: `/Users/anicca/anicca`
- Branch/HEAD/upstream: `feature/dist1-mcp-launchd` / `dbd673b0855b868978a7b3d632d56584982628cd` / 同一。
- Dirty warning: tracked runtime logs 3本だけがmodified（`skills/earn/x402-sell/logs/x402-experiment-franklin1.out.log`, `x402-franklin1.out.log`, `x402-franklin2.out.log`）。stageしない。
- Docs SSOT remote: `Daisuke134/anicca-products` branch `feature/clip-rewards`; handover直前のverified spec HEADは `6d48ae89b2f6c8304fa71caffcf2f809769036d6`。このhandover/spec regression correctionを含む最新remote HEADを再開時にfetchして使う。
- Active workers: 無し。最後のread-only reviewerはawesome-x402画像追記をPASS。
- Runtime: image 3/3 と MCP 3/3 LaunchAgentがrunning。正本image URLは443 `/image`、`:10000/image`、`:8443/image`で全て402。MCP 3本はno-sessionで400。別名host `franklin1/franklin2/claude-p.tail7a0ba4.ts.net/image` は404なので使わない。

## Completed evidence and public side effects

- x402scan image listing 3件:
  - https://www.x402scan.com/server/7e8ebdc1-7c3d-419f-b76b-e41bab7bb86c
  - https://www.x402scan.com/server/af9283bc-b1f8-4e50-b474-abb1f5d082e0
  - https://www.x402scan.com/server/439753c7-81e9-4c3e-b383-3be9c7377d9e
- awesome-x402 PR #838はOPEN/MERGEABLE/CLEAN、signed head `93041943352b4fb56961e3850ca12aa3e2a38ce6`。3店の$0.05 `POST /image`を追記済み。
- Questflow resource PR #11はOPEN/MERGEABLE/CLEAN、Fluora issue #3とMCPay issue #49はOPEN/comment 0。
- Agentic Marketは既存Anicca endpointを自動掲載。image 3本はvalidator必須項目greenだが `found=false`。最初の実verify+settle後までBazaar検索に出ない。
- Coinbase ecosystemは廃止、Pay.shはSolana必須、Onchain.fiはmarketplace無し、AmpersendはBazaar集約のため追加submit無し。重複PRを作らない。
- 48h Base USDC scan `48895683..48982083`: 3 wallet全て `inflows=0, selfPay=0, EXTERNAL=0, externalUsdc=0`。実収益は$0 / ¥0。

## Active failure and first safe resume action

- Fresh suiteは184中183 pass。並列full、直列full、対象file単独の全てで `image resale sends no inherited human credentials upstream` が `image-resale.test.mjs:134` の `Object.keys(undefined)`でfail。原因未確定。
- 最初の安全な再開: 現在のHEADでREDを再現し、testとimage resale実装の契約を読んで、credential isolationを弱めずTDDで184/184へ戻す。その後public 402/400と48h inflowを再検証し、B4の外部buyer acquisition/観測へ戻る。
- Protected boundaries: self-pay/送金で証拠を作らない、human credential/KYCを使わない、`serve-v2.mjs`無変更、既存Funnel mountを削除しない、prompt/payment header/private keyを記録しない。

## Copy-paste /goal

/goal
Make Anicca’s live x402 earning lane produce repeatable, verifiable third-party USDC revenue for self-funded agents without human credentials or human-in-the-loop operation. Read `/Users/anicca/anicca-project/.claude/handovers/2026-07-23_0553_x402-b4-b5.md` and `/Users/anicca/anicca-project/docs/superpowers/specs/2026-07-18-bounty-loop-onchain-spec.md` first, verify both against current git/runtime/external state, and treat the spec’s B4→B5 table plus `OPEN RISK / honest gap` as the remaining-TODO SSOT.

Before acquisition work, reproduce and repair the current 183/184 regression in `image-resale.test.mjs` with TDD; credential isolation must remain fail-closed and tests must not be weakened. Done requires fresh evidence that: (1) all x402-sell tests pass; (2) all three image LaunchAgents and MCP LaunchAgents are running, public image URLs return valid x402 v2 402 challenges, and public MCP no-session requests return 400; (3) a real third-party buyer—not any SELF_WALLETS address or self-initiated protocol return—purchases an image/x402 product and a later external purchase proves the loop repeats; (4) each qualifying transaction is finalized on Base, has a successful receipt and exact USDC Transfer to the serving agent’s payTo, matches settled sales telemetry, and is written exactly once by the wallet-specific recorder; (5) realized revenue and upstream/gas cost demonstrate positive margin; and (6) after the first real settle, the sold image endpoint is actually searchable in Agentic Market/Bazaar or the external index state is reported honestly as blocked. PRs/issues/listings are distribution evidence, never revenue.

Never self-pay, wash trade, transfer wallet funds to manufacture evidence, use Dais’s human credentials/KYC/bank accounts, expose private keys/prompts/payment headers, modify `serve-v2.mjs`, remove existing Funnel mounts, or count submit/PR/merge/pending payout as earnings. Preserve the three isolated payTo wallets and existing HTTP x402 behavior. Research active buyer demand before changing product/price/metadata; reject dead, incompatible, or supply-only directories. Keep bounty rails monitor-only unless automation policy, no-human identity, live eligible supply, and direct crypto payout all pass.

After every evidence pass, update the spec with observed values, commit and push only owned paths, and continue with the highest-risk open item instead of ending on a plan. Use `spawn_agent` only for bounded read-only demand research or independent review that could change Done; the parent owns edits, external side effects, and fresh verification. Correctness/security/earnings-evidence findings block Done; minor review suggestions are discretionary and must be recorded if skipped. If three materially distinct safe acquisition approaches produce no external buyer, or a required external service/credential is unavailable with no honest substitute, stop with a pushed handover containing attempts, evidence, exact blocker, and smallest restart action. Report $0 / ¥0 whenever verified external revenue is zero.
