# 1.6.2 TODO-NEXT（唯一の正本 / SSOT）

最終更新: 2026-02-11  
対象ブランチ: `codex/1.6.2-ssot-skills-crons`

このファイルの役割:
- 1.6.2で「何をやるか / 何をやらないか / 何をもって完了とするか」を1ファイルで固定する。
- 完了判定はこのファイルのチェックのみを使う（未完ゼロで完了）。
- リリース報告（X記事）はこのファイルの「完了チェック + 証跡」だけをソースにする。

---

## 0. SSOTルール（絶対）

- [ ] 本番運用SSOTは VPS `(/home/anicca/.openclaw)` のみ。
- [ ] Slack接続する Gateway は1台のみ（重複返信禁止）。
- [ ] 送信経路は OpenClaw cron のみ（GHA送信系 / Railway送信cron は停止）。
- [ ] Done定義は「コード + 運用接続 + 証跡（runs/log/audit）」。
- [ ] 1.6.2の完了判定は本ファイルの未完ゼロのみ。

プラットフォームポリシー:
- X: 投稿のみ（返信禁止）。
- TikTok: 投稿のみ。
- Moltbook: 投稿 + 返信（ただしSAFE-T優先）。
- iOS: センサー非依存（固定スロット + server-driven inbox）。

---

## 1. スコープ境界（1.6.2 / 1.6.3）

### 1.6.2でやる（今回完了対象）

- [ ] 閉ループ運用のSSOT化（`proposal -> mission -> steps -> events -> heartbeat`）。
- [ ] 全ロジックのスキル割当（未割当ゼロ）。
- [ ] VPSへの全 `SKILL.md` 配置。
- [ ] `jobs.json` 統一（cron一本化）。
- [ ] 二重送信ゼロ化（GHA投稿系停止 / Railway送信cron停止）。
- [ ] `app-nudge-sender` 1日3スロット固定。
- [ ] `moltbook-poster` 実稼働（dry_run -> 本番）。
- [ ] E2E合格（crisis / iOS / X / TikTok / Moltbook）。

### 1.6.3に送る（今回の範囲外）

- [ ] Voice Evolution / Dynamic Affinity。
- [ ] Onboarding / Ads / Paywall の最適化ループ。
- [ ] 長期自律運用（2週間連続運転）など拡張運用。

---

## 2. To-Be スキル一覧（1.6.2確定）

- [ ] `ops-heartbeat`: `POST /api/ops/heartbeat`。trigger/reaction/recoveryの制御プレーン。
- [ ] `mission-worker`: `GET /api/ops/step/next` -> step実行 -> `PATCH /api/ops/step/:id/complete`。
- [ ] `trend-hunter`: `run_trend_scan` proposal起票（話題収集 + hook候補 + event）。
- [ ] `suffering-detector`: `detect_suffering` proposal起票（severity判定 + SAFE-T）。
- [ ] `x-poster`: `draft_content -> verify_content -> post_x` proposal起票。
- [ ] `tiktok-poster`: `draft_content -> verify_content -> post_tiktok` proposal起票。
- [ ] `app-nudge-sender`: `draft_nudge -> send_nudge` proposal起票。
- [ ] `moltbook-monitor`: 監視入口（event発火、shadow可）。
- [ ] `moltbook-poster`: 投稿/返信 proposal起票（実送信まで）。
- [ ] `roundtable-standup` / `roundtable-memory-extract` / `roundtable-initiative-generate`。
- [ ] `hookpost-ttl-cleaner` / `sto-weekly-refresh` / `autonomy-check`。

---

## 3. To-Be Cron一覧（JST確定）

- [ ] `ops-heartbeat` -> `*/5 * * * *`
- [ ] `mission-worker` -> `* * * * *`
- [ ] `trend-hunter` -> `0 */4 * * *`
- [ ] `suffering-detector` -> `*/5 * * * *`
- [ ] `x-poster-morning` -> `0 9 * * *`
- [ ] `x-poster-evening` -> `0 21 * * *`
- [ ] `tiktok-poster-morning` -> `0 9 * * *`
- [ ] `tiktok-poster-evening` -> `0 21 * * *`
- [ ] `app-nudge-morning` -> `0 9 * * *`
- [ ] `app-nudge-afternoon` -> `0 14 * * *`
- [ ] `app-nudge-evening` -> `0 20 * * *`
- [ ] `moltbook-monitor` -> `*/5 * * * *`
- [ ] `moltbook-poster` -> `30 20 * * *`
- [ ] `roundtable-standup` -> `0 9 * * *`
- [ ] `roundtable-memory-extract` -> `55 8 * * *`
- [ ] `roundtable-initiative-generate` -> `5 9 * * *`
- [ ] `hookpost-ttl-cleaner` -> `0 3 * * *`
- [ ] `sto-weekly-refresh` -> `0 3 * * 0`
- [ ] `autonomy-check` -> `0 3 * * *`

---

## 4. 実装タスク（エージェント実装担当）

### 4.1 スキル化・設定

- [ ] スキル割当マップ作成（ロジック->スキル 1:1）。
- [ ] 各 `SKILL.md` 必須要素（目的/env/tools/input/output/error/policy）を充足。
- [x] VPS `/home/anicca/.openclaw/skills/<skill>/SKILL.md` へ全配置。
- [x] `jobs.json` を本ファイルのCronに一致させる。
- [ ] 必須env投入:
- [ ] `API_BASE_URL`
- [ ] `ANICCA_AGENT_TOKEN`
- [ ] `APIFY_API_TOKEN`
- [ ] `TWITTERAPI_KEY`
- [ ] `REDDAPI_API_KEY`
- [ ] `MOLTBOOK_BASE_URL`
- [ ] `MOLTBOOK_ACCESS_TOKEN`
- [x] gateway再起動 + skill/cron読込確認。

### 4.2 重複経路停止

- [x] 投稿系 GHA を停止（schedule削除、workflow_dispatchのみ）:
- [x] `.github/workflows/anicca-x-post.yml`
- [x] `.github/workflows/anicca-daily-post.yml`
- [x] `.github/workflows/tiktok-card-post.yml`
- [x] `.github/workflows/cross-post-tiktok-to-ig.yml`
- [ ] 計測系 workflow は「送信をしない」形で分離確認:
- [ ] `.github/workflows/fetch-x-metrics.yml`
- [ ] `.github/workflows/fetch-tiktok-metrics.yml`
- [ ] Railway送信cron停止（送信責務をOpenClawへ一本化）。

### 4.3 未完ポイント潰し

- [ ] Moltbook返信ポリシー確定（opt-in/頻度/上限/禁止表現/SAFE-T中断）。
- [ ] `moltbook-poster` dry_run成功。
- [ ] `moltbook-poster` 本番送信成功。
- [ ] `moltbook-monitor` -> reply/post mission接続確認。
- [ ] `app-nudge-sender` 3スロット運用でruns/auditを確認。
- [ ] initiative-runner接続（C1の運用側未完解消）。

---

## 5. テスト指標（AniccaはE2E専任）

注記:
- 実装・修正はこのブランチで実施。
- 最終E2E実行は Anicca（VPS実体）で実施。

### 5.1 事前テスト（実装側）

- [x] API/ops系テストが緑（回帰なし）。（vitest 34file 133test pass）
- [ ] スキル dry_run 全件成功。
- [x] `jobs.json` ロードエラーゼロ。（VPS反映後確認: jq ".jobs | length" → 19）

### 5.2 E2Eメトリクス（Anicca実行）

- [ ] **重複送信**: 同一 `dedupe_key` 重複数 = `0`
- [ ] **closed-loop完走率**: mission success rate >= `95%`
- [ ] **stale step**: `0`
- [ ] **crisis遅延**: `crisis:detected` -> Slack通知 <= `60s`
- [ ] **iOS配信**: `pending -> ack` 成功率 >= `95%`
- [ ] **X投稿**: post成功率 >= `95%`, 返信件数 `0`
- [ ] **TikTok投稿**: post成功率 >= `95%`
- [ ] **Moltbook実送信**: success率 >= `95%`

### 5.3 E2E合格証跡（必須提出）

- [ ] OpenClaw `cron/runs` ログ
- [ ] API監査ログ（proposal/mission/step/events）
- [ ] Slack通知証跡（crisis/autonomy）
- [ ] iOS側 pending/ack レスポンス記録
- [ ] X/TikTok/Moltbook の投稿ID・メトリクス

---

## 6. 受け入れ基準（DoD）

- [ ] 全ロジックのスキル割当が完了（未割当ゼロ）。
- [ ] VPSに全 `SKILL.md` 配置済み。
- [ ] `jobs.json` が本ファイルと一致。
- [ ] 送信経路一本化（OpenClawのみ）。
- [ ] スキル単体テスト（dry_run含む）合格。
- [ ] E2E（crisis / iOS / X / TikTok / Moltbook）合格。
- [ ] 本ファイルの未完チェックがゼロ。

---

## 7. マージ前チェック

- [ ] ブランチ: `codex/1.6.2-ssot-skills-crons`
- [ ] 差分に不要ファイルなし（実装対象のみ）
- [ ] TODO未完ゼロ
- [ ] Codexレビュー完了（実装後のレビューゲート）
- [ ] `dev` へマージ可能判定 = YES

---

## 8. リリース記事ソース（X記事用）

このセクションは完了時に埋める:
- [ ] 何を直したか（重複送信ゼロ化、スキル統一、E2E完了）
- [ ] どの証跡で確認したか（runs/logs/audit）
- [ ] 主要メトリクス結果（5.2）
- [ ] 1.6.3へ送った項目（範囲外）
