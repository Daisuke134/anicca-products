# 1.6.2 実装後 TODO（運用移行・未実装解消）

最終更新: 2026-02-09  
対象: `1.6.2` 実装完了後の残タスクを、他エージェントがそのまま実行できる形で整理

---

## 現在地（要約）

- Phase 1（X投稿→計測→学習ループ）は稼働中。
- `ops` 基盤（Proposal/Mission/Step/Trigger/Reaction）は実装済み。
- ただし `detect_suffering` / `app-nudge-sender` / Moltbook導線は仕様先行で、実働統合が未完。
- ドキュメントに参照パスずれと完了ステータス不整合が残る。

---

## 優先度A（今すぐ）

## A1. ドキュメント整合（SSOT修正）
- [ ] `.cursor/plans/reference/openclaw-anicca.md` の `Slack実地検証` を完了反映
- [ ] `implementation/deployment-todo.md` 参照を `../deployment-todo.md` に修正
- [ ] 「現行稼働Cron」に `ops-heartbeat` 以外の未稼働項目を誤記しないよう明示

完了条件:
- `openclaw-anicca.md` だけ読めば、現在の稼働/未稼働が誤解なく分かる。

---

## 優先度A（機能ギャップ解消）

## A2. `detect_suffering` を仕様どおり運用可能化
- [ ] VPS `suffering-detector` スキル実体を作成（`detections[]` を `step/complete` に返す）
- [ ] `severity >= 0.9` で `crisis:detected`、`>=0.6` で `suffering_detected` を実際に発火
- [ ] SAFE-T 時の通常フロー中断 + アラート経路（Slack）を検証

参照:
- `closed-loop-ops/05-step-executors.md`（SAFE-T, step/completeイベント化）
- `closed-loop-ops/02-data-layer.md`（`suffering_nudge` trigger rule）

完了条件:
- 検出サンプル投入で `ops_events` に期待イベントが出る。
- 危機ケースで Nudge系通常提案が流れない。

## A3. `app-nudge-sender` 経路の実接続
- [ ] 仕様にある送信API経路を実装（現行ルートとの差分解消）
- [ ] `draft_nudge -> send_nudge` の実送信部分を VPS Worker から呼べるよう統一
- [ ] `nudge_daily_quota` と Kill Switch の挙動をE2Eで確認

現状ギャップ:
- 仕様には `/api/mobile/nudge/send` 記述あり。現コード側は同名エンドポイント未確認。

完了条件:
- `suffering_detected` から実際に iOS 通知まで届く。

## A4. Moltbook導線の運用化
- [ ] Moltbook監視→苦しみ検出→返信（Moltbook内）の最小フローを本番前提で固定
- [ ] Xは「検出のみ・返信しない・App Nudgeへ回す」方針をコード/設定で強制
- [ ] プラットフォーム別ポリシーを `AGENTS.md`/ops docs に明記

完了条件:
- MoltbookとXで挙動差が仕様どおりに再現される。

---

## 優先度B（安定化）

## B1. Trigger評価ロジックの既知バグ修正
- [ ] `triggerEvaluator` の「最新イベントのみ評価」問題を修正（全マッチイベント評価）
- [ ] 回帰テスト追加（古いイベントが新しいイベントで潰れない）

完了条件:
- 複数イベント同時発生時も取りこぼしなし。

## B2. X/TikTok投稿安定化
- [ ] Blotato長文失敗に対するガード（文字数/権限/再試行戦略）
- [ ] X APIクレジット枯渇時の運用ルールを明文化（fallback時の扱い統一）
- [ ] TikTok本接続（Phase 3）に向けた API 実装準備

完了条件:
- 失敗理由が運用で追跡可能、再現手順と対応が固定される。

---

## 優先度C（Phase 2開始条件）

## C1. 3エージェント化着手（P2-1〜P2-4）
- [ ] `anicca / hunter / growth` の責務定義
- [ ] `openclaw.json agents[]` 追加
- [ ] Roundtable（standup/debate/watercooler）実装
- [ ] 09:00 JST の standup Cron を追加（#ops出力）

## C2. 学習強化（P2-5〜P2-10）
- [ ] 構造化Memoryテーブル追加
- [ ] 会話ログ→Memory抽出
- [ ] Initiative（自発Proposal）
- [ ] x-research 統合
- [ ] Reaction Matrix設定
- [ ] 1週間自律運用テスト

完了条件:
- 人手は承認中心、提案は自発で日次運転できる。

---

## 実行順（推奨）

1. A1（ドキュメント整合）
2. A2（detect_suffering実働化）
3. A3（app-nudge-sender実接続）
4. A4（Moltbook運用化）
5. B1（triggerEvaluator修正）
6. B2（投稿安定化）
7. C1/C2（Phase 2）

---

## 引き継ぎメモ（他エージェント向け）

- `1.6.2` の正本は `implementation/` 配下。古いアーカイブ参照を避ける。
- `send_nudge` は公開介入扱い。Kill Switchと承認フローを外さない。
- SAFE-T 関連は機能追加より優先（危機判定の誤動作は最優先で止める）。
