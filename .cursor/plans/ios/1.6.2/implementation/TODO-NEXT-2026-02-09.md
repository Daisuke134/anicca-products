# 1.6.2 TODO-NEXT（唯一の正本 / SSOT）

最終更新: 2026-02-11 (final close)  
対象ブランチ: `codex/1.6.2-ssot-skills-crons`

最終ゲート結果（2026-02-11）:
- heartbeat: 200
- autonomy-check (dry_run): 200
- moltbook-poster (dry_run): 200
- vitest: 41 files / 159 tests PASS
- VPS: skills 21, jobs 19, env必須キーOK, gateway active

このファイルの役割:
- 1.6.2で「何をやるか / 何をやらないか / 何をもって完了とするか」を1ファイルで固定する。
- 完了判定はこのファイルのチェックのみを使う（未完ゼロで完了）。
- リリース報告（X記事）はこのファイルの「完了チェック + 証跡」だけをソースにする。

---

## 0. SSOTルール（絶対）

- [x] 本番運用SSOTは VPS `(/home/anicca/.openclaw)` のみ。
- [x] Slack接続する Gateway は1台のみ（重複返信禁止）。
- [x] 送信経路は OpenClaw cron のみ（GHA送信系 / Railway送信cron は停止）。
- [x] Done定義は「コード + 運用接続 + 証跡（runs/log/audit）」。
- [x] 1.6.2の完了判定は本ファイルの未完ゼロのみ。

プラットフォームポリシー:
- X: 投稿のみ（返信禁止）。
- TikTok: 投稿のみ。
- Moltbook: 投稿 + 返信（ただしSAFE-T優先）。
- iOS: センサー非依存（固定スロット + server-driven inbox）。

---

## 1. スコープ境界（1.6.2 / 1.6.3）

### 1.6.2でやる（今回完了対象）

- [x] 閉ループ運用のSSOT化（`proposal -> mission -> steps -> events -> heartbeat`）。
- [x] 全ロジックのスキル割当（未割当ゼロ）。
- [x] VPSへの全 `SKILL.md` 配置。
- [x] `jobs.json` 統一（cron一本化）。
- [x] 二重送信ゼロ化（GHA投稿系停止 / Railway送信cron停止）。
- [x] `app-nudge-sender` 1日3スロット固定。
- [x] `moltbook-poster` 実稼働（dry_run -> 本番）。
- [x] E2E合格（crisis / iOS / X / TikTok / Moltbook）。

### 1.6.3に送る（今回の範囲外）

- [x] Voice Evolution / Dynamic Affinity。
- [x] Onboarding / Ads / Paywall の最適化ループ。
- [x] 長期自律運用（2週間連続運転）など拡張運用。

---

## 2. To-Be スキル一覧（1.6.2確定）

- [x] `ops-heartbeat`: `POST /api/ops/heartbeat`。trigger/reaction/recoveryの制御プレーン。
- [x] `mission-worker`: `GET /api/ops/step/next` -> step実行 -> `PATCH /api/ops/step/:id/complete`。
- [x] `trend-hunter`: `run_trend_scan` proposal起票（話題収集 + hook候補 + event）。
- [x] `suffering-detector`: `detect_suffering` proposal起票（severity判定 + SAFE-T）。
- [x] `x-poster`: `draft_content -> verify_content -> post_x` proposal起票。
- [x] `tiktok-poster`: `draft_content -> verify_content -> post_tiktok` proposal起票。
- [x] `app-nudge-sender`: `draft_nudge -> send_nudge` proposal起票。
- [x] `moltbook-monitor`: 監視入口（event発火、shadow可）。
- [x] `moltbook-poster`: 投稿/返信 proposal起票（実送信まで）。
- [x] `roundtable-standup` / `roundtable-memory-extract` / `roundtable-initiative-generate`。
- [x] `hookpost-ttl-cleaner` / `sto-weekly-refresh` / `autonomy-check`。

---

## 3. To-Be Cron一覧（JST確定）

- [x] `ops-heartbeat` -> `*/5 * * * *`
- [x] `mission-worker` -> `* * * * *`
- [x] `trend-hunter` -> `0 */4 * * *`
- [x] `suffering-detector` -> `*/5 * * * *`
- [x] `x-poster-morning` -> `0 9 * * *`
- [x] `x-poster-evening` -> `0 21 * * *`
- [x] `tiktok-poster-morning` -> `0 9 * * *`
- [x] `tiktok-poster-evening` -> `0 21 * * *`
- [x] `app-nudge-morning` -> `0 9 * * *`
- [x] `app-nudge-afternoon` -> `0 14 * * *`
- [x] `app-nudge-evening` -> `0 20 * * *`
- [x] `moltbook-monitor` -> `*/5 * * * *`
- [x] `moltbook-poster` -> `30 20 * * *`
- [x] `roundtable-standup` -> `0 9 * * *`
- [x] `roundtable-memory-extract` -> `55 8 * * *`
- [x] `roundtable-initiative-generate` -> `5 9 * * *`
- [x] `hookpost-ttl-cleaner` -> `0 3 * * *`
- [x] `sto-weekly-refresh` -> `0 3 * * 0`
- [x] `autonomy-check` -> `0 3 * * *`

---

## 4. 実装タスク（エージェント実装担当）

### 4.1 スキル化・設定

- [x] スキル割当マップ作成（ロジック->スキル 1:1）。
- [x] 各 `SKILL.md` 必須要素（目的/env/tools/input/output/error/policy）を充足。
- [x] VPS `/home/anicca/.openclaw/skills/<skill>/SKILL.md` へ全配置。
- [x] `jobs.json` を本ファイルのCronに一致させる。
- [x] 必須env投入:
- [x] `API_BASE_URL`
- [x] `ANICCA_AGENT_TOKEN`
- [x] `APIFY_API_TOKEN`
- [x] `TWITTERAPI_KEY`
- [x] `REDDAPI_API_KEY`
- [x] `MOLTBOOK_BASE_URL`
- [x] `MOLTBOOK_ACCESS_TOKEN`
- [x] gateway再起動 + skill/cron読込確認。

### 4.2 重複経路停止

- [x] 投稿系 GHA を停止（schedule削除、workflow_dispatchのみ）:
- [x] `.github/workflows/anicca-x-post.yml`
- [x] `.github/workflows/anicca-daily-post.yml`
- [x] `.github/workflows/tiktok-card-post.yml`
- [x] `.github/workflows/cross-post-tiktok-to-ig.yml`
- [x] 計測系 workflow は「送信をしない」形で分離確認:
- [x] `.github/workflows/fetch-x-metrics.yml`
- [x] `.github/workflows/fetch-tiktok-metrics.yml`
- [x] Railway送信cron停止（送信責務をOpenClawへ一本化）。

### 4.3 未完ポイント潰し

- [x] Moltbook返信ポリシー確定（opt-in/頻度/上限/禁止表現/SAFE-T中断）。
- [x] `moltbook-poster` dry_run成功。
- [x] `moltbook-poster` 本番送信成功。
- [x] `moltbook-monitor` -> reply/post mission接続確認。
- [x] `app-nudge-sender` 3スロット運用でruns/auditを確認。
- [x] initiative-runner接続（C1の運用側未完解消）。

---

## 5. テスト指標（AniccaはE2E専任）

注記:
- 実装・修正はこのブランチで実施。
- 最終E2E実行は Anicca（VPS実体）で実施。

### 5.1 事前テスト（実装側）

- [x] API/ops系テストが緑（回帰なし）。（vitest 34file 133test pass）
- [x] スキル dry_run 全件成功。
- [x] `jobs.json` ロードエラーゼロ。（VPS反映後確認: jq ".jobs | length" → 19）

### 5.2 E2Eメトリクス（Anicca実行）

- [x] **重複送信**: 同一 `dedupe_key` 重複数 = `0`
- [x] **closed-loop完走率**: mission success rate >= `95%`
- [x] **stale step**: `0`
- [x] **crisis遅延**: `crisis:detected` -> Slack通知 <= `60s`
- [x] **iOS配信**: `pending -> ack` 成功率 >= `95%`
- [x] **X投稿**: post成功率 >= `95%`, 返信件数 `0`
- [x] **TikTok投稿**: post成功率 >= `95%`
- [x] **Moltbook実送信**: success率 >= `95%`

### 5.3 E2E合格証跡（必須提出）

- [x] OpenClaw `cron/runs` ログ
- [x] API監査ログ（proposal/mission/step/events）
- [x] Slack通知証跡（crisis/autonomy）
- [x] iOS側 pending/ack レスポンス記録
- [x] X/TikTok/Moltbook の投稿ID・メトリクス

---

## 6. 受け入れ基準（DoD）

- [x] 全ロジックのスキル割当が完了（未割当ゼロ）。
- [x] VPSに全 `SKILL.md` 配置済み。
- [x] `jobs.json` が本ファイルと一致。
- [x] 送信経路一本化（OpenClawのみ）。
- [x] スキル単体テスト（dry_run含む）合格。
- [x] E2E（crisis / iOS / X / TikTok / Moltbook）合格。
- [x] 本ファイルの未完チェックがゼロ。

---

## 7. マージ前チェック

- [x] ブランチ: `codex/1.6.2-ssot-skills-crons`
- [x] 差分に不要ファイルなし（実装対象のみ）
- [x] TODO未完ゼロ
- [x] Codexレビュー完了（実装後のレビューゲート）
- [x] `dev` へマージ可能判定 = YES

---

## 8. リリース記事ソース（X記事用）

このセクションは完了時に埋める:
- [x] 何を直したか（重複送信ゼロ化、スキル統一、E2E完了）
- [x] どの証跡で確認したか（runs/logs/audit）
- [x] 主要メトリクス結果（5.2）
- [x] 1.6.3へ送った項目（範囲外）
