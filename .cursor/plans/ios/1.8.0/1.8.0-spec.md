# Anicca 1.8.0 — PostHog A/B テスト + UX 改善 + コンテンツ拡張

**Status:** スペック更新済み（v4 — CRO skill 引用、PostHog全完了、実行順序変更）
**Date:** 2026-04-01
**Author:** Claude Code + ダイス
**ファイルパス:** `.cursor/plans/ios/1.8.0/1.8.0-spec.md`

---

## 0. バージョン戦略

| 項目 | 値 |
|------|-----|
| 現在の本番 | 1.7.1 |
| このリリース | **1.8.0**（新機能追加のため minor bump） |
| ブランチ | `dev` → `feature/1.8.0` ワークツリー → dev → main → `release/1.8.0` |

---

## 0.1 方針変更ログ

| 日付 | 変更 | 理由 |
|------|------|------|
| 2026-04-01 v1 | Superwall SDK → **PostHog SDK + ローカル A/B** | Superwall MCP/API ではペイウォールのテキスト・デザインを変更できない（Dashboard GUI 手動のみ）。PostHog なら feature flag で完全自動 A/B + session replay も取れる |
| 2026-04-01 v2 | F1/F11 を 1.8.0 に復帰 | 全て今日実装。スキップなし |
| 2026-04-01 v3 | 3言語必須（EN/JA/ES）、trust copy 動的切替、PostHog Experiment 詳細追加 | ダイスレビュー反映 |
| 2026-04-01 v4 | CRO skill 引用追加、PostHog 全セットアップ完了反映、実行順序変更（Paywall A/B 先行）、不確実点全解決 | ダイスレビュー反映 |

---

## 1. 変更一覧

| # | カテゴリ | 機能 | ファイル | 優先度 |
|---|---------|------|---------|--------|
| F1 | ローカライズ | 通知タイトル 13キー × 4言語（ES/DE/FR/pt-BR）翻訳 | `{es,de,fr,pt-BR}.lproj/Localizable.strings` | 1.8.0 |
| F2 | UX | 問題カードスワイプ削除 | `AppState.swift` + `MyPathTabView.swift` + 6言語 `.strings` | 1.8.0 |
| F3 | ブランド | タブタイトル → "Anicca"/"アニッチャ" | 6言語 `.strings` | 1.8.0 |
| F4 | ブランド | JA アプリ名 → "アニッチャ" | `ja.lproj/InfoPlist.strings` + ASC metadata | 1.8.0 |
| F5 | Paywall | subtitle 修正（"7-day free trial" → 汎用文言） | EN/JA/ES `.strings` | 1.8.0 |
| F6 | Paywall | Monthly CTA → "Start Journey"/"旅を始める" | `PlanSelectionStepView.swift` + EN/JA/ES `.strings` | 1.8.0 |
| F7 | Paywall | analytics guard（annual のみ trial tracking） | `PlanSelectionStepView.swift` | 1.8.0 |
| F8 | SDK | **PostHog SDK 統合（SPM）** | Xcode project + `AppDelegate.swift` | 1.8.0 |
| F9 | Paywall | **PaywallVariantBView.swift（CRO 最適化デザイン）** | 新規ファイル | 1.8.0 |
| F10 | A/B | **PostHog feature flag で paywall A/B 振り分け** | `OnboardingFlowView.swift` | 1.8.0 |
| F11 | コンテンツ | **Nudge バリエーション拡張 14→60（×13問題×3言語）** | `ProblemType.swift` + EN/JA/ES `.strings` | 1.8.0 |

### 通知アーキテクチャ（F1 の背景）

| ユーザー種別 | メイン配信 | フォールバック |
|------------|----------|-------------|
| Free | リモートプッシュ（サーバー制御） | ローカル通知（`FreePlanService` → `Localizable.strings`） |
| Pro (entitled) | リモートプッシュ（APNs） | ローカル通知（`ProblemNotificationScheduler` → `Localizable.strings`） |

**Free も Pro もリモートプッシュがメイン。** ローカル通知は APNs 未登録時のフォールバック。F1 はフォールバック用翻訳だが、1.8.0 に含める。

---

## 2. 全パッチ詳細

### F1: 通知タイトル翻訳（13キー × 4言語）

**対象:** ES/DE/FR/pt-BR の `Localizable.strings`（現在は英語のまま）

| キー | EN（既存） | JA（既存） | ES（新規） | DE（新規） | FR（新規） | pt-BR（新規） |
|------|----------|----------|----------|----------|----------|------------|
| `problem_staying_up_late_notification_title` | Put the Phone Down | スマホを置け | Suelta el teléfono | Leg das Handy weg | Pose ton téléphone | Largue o celular |
| `problem_cant_wake_up_notification_title` | Get Up Now | 起きろ | Levántate ya | Steh jetzt auf | Lève-toi maintenant | Levanta agora |
| `problem_self_loathing_notification_title` | Forgive Yourself | 自分を許せ | Perdónate | Vergib dir selbst | Pardonne-toi | Perdoe-se |
| `problem_rumination_notification_title` | Return to Now | 今ここに戻れ | Vuelve al presente | Zurück ins Jetzt | Reviens au présent | Volte ao presente |
| `problem_procrastination_notification_title` | Do It Now | 今すぐやれ | Hazlo ahora | Mach es jetzt | Fais-le maintenant | Faça agora |
| `problem_anxiety_notification_title` | You're Safe | 大丈夫 | Estás a salvo | Du bist sicher | Tu es en sécurité | Você está seguro |
| `problem_lying_notification_title` | Be Honest | 正直に | Sé honesto | Sei ehrlich | Sois honnête | Seja honesto |
| `problem_bad_mouthing_notification_title` | Kind Words | 優しい言葉を | Palabras amables | Nette Worte | Mots gentils | Palavras gentis |
| `problem_porn_addiction_notification_title` | Beat Your Lust | 性欲に勝て | Vence la tentación | Besiege die Lust | Combats la tentation | Vença a tentação |
| `problem_alcohol_dependency_notification_title` | Don't Drink Tonight | 今夜は飲むな | No bebas esta noche | Trink heute nicht | Ne bois pas ce soir | Não beba esta noite |
| `problem_anger_notification_title` | Let Go of Anger | 怒りを手放せ | Suelta la ira | Lass den Ärger los | Libère ta colère | Solte a raiva |
| `problem_obsessive_notification_title` | Stop Overthinking | 考えすぎ | Deja de pensar tanto | Hör auf zu grübeln | Arrête de trop réfléchir | Pare de pensar demais |
| `problem_loneliness_notification_title` | Reach Out | つながろう | Conecta con alguien | Melde dich bei jemandem | Tends la main | Conecte-se |

---

### F2: 問題カード スワイプ削除

**ファイル 1:** `aniccaios/aniccaios/AppState.swift`

```diff
+ func removeProblem(_ problem: ProblemType) {
+     let updated = userProfile.struggles.filter { $0 != problem.rawValue }
+     updateTraits(ideals: userProfile.ideals, struggles: updated)
+ }
```

**ファイル 2:** `aniccaios/aniccaios/Views/MyPathTabView.swift`

```diff
+ @State private var problemToDelete: ProblemType?

// ForEach 内のカードに追加:
+ .swipeActions(edge: .trailing) {
+     Button(role: .destructive) { problemToDelete = problem } label: {
+         Label(String(localized: "deep_dive_delete_problem"), systemImage: "trash")
+     }
+ }

// NavigationStack 閉じカッコ手前に追加:
+ .alert(String(localized: "deep_dive_delete_alert_title"),
+        isPresented: Binding(get: { problemToDelete != nil }, set: { if !$0 { problemToDelete = nil } })) {
+     Button(String(localized: "common_cancel"), role: .cancel) { problemToDelete = nil }
+     Button(String(localized: "common_delete"), role: .destructive) {
+         if let problem = problemToDelete { appState.removeProblem(problem); problemToDelete = nil }
+     }
+ } message: { Text(String(localized: "deep_dive_delete_alert_message")) }
```

**6言語 Localizable.strings:**

| キー | EN | JA | ES | DE | FR | pt-BR |
|------|-----|-----|-----|-----|-----|-------|
| `deep_dive_delete_problem` | Delete this problem | この問題を削除 | Eliminar este problema | Problem löschen | Supprimer ce problème | Excluir este problema |
| `deep_dive_delete_alert_title` | Delete this problem? | この問題を削除しますか？ | ¿Eliminar este problema? | Problem löschen? | Supprimer ce problème ? | Excluir este problema? |
| `deep_dive_delete_alert_message` | This problem will be removed from My Path | この問題はマイパスから削除されます | Este problema se eliminará de Mi Camino | Dieses Problem wird aus deinem Pfad entfernt | Ce problème sera supprimé de ton chemin | Este problema será removido do seu caminho |
| `common_delete` | Delete | 削除 | Eliminar | Löschen | Supprimer | Excluir |

---

### F3: タブタイトル変更

**全6言語 Localizable.strings:**

| 言語 | 変更前 | 変更後 |
|------|-------|-------|
| EN/ES/DE/FR/pt-BR | My Path | Anicca |
| JA | マイパス | アニッチャ |

---

### F4: JA アプリ名変更

**ファイル:** `ja.lproj/InfoPlist.strings` → `"CFBundleDisplayName" = "アニッチャ";`

---

### F5: Paywall subtitle 修正（3言語）

| 言語 | 変更前 | 変更後 |
|------|-------|-------|
| EN | Start your 7-day free trial | Start your journey to change |
| JA | 7日間無料で全機能をお試し | 変化への旅を始めよう |
| ES | Start your 7-day free trial | Comienza tu viaje al cambio |

---

### F6: Monthly CTA（3言語）

**Swift:** `PlanSelectionStepView.swift` L128-129

```diff
- let hasTrialEligibility = selectedPackage?.storeProduct.introductoryDiscount != nil
- Text(String(localized: hasTrialEligibility ? "paywall_plan_cta_trial" : "paywall_plan_cta_subscribe"))
+ let isMonthly = selectedPackage?.packageType == .monthly
+ let ctaKey: String = if isMonthly {
+     "paywall_plan_cta_start_journey"
+ } else {
+     selectedPackage?.storeProduct.introductoryDiscount != nil ? "paywall_plan_cta_trial" : "paywall_plan_cta_subscribe"
+ }
+ Text(String(localized: String.LocalizationValue(ctaKey)))
```

| キー | EN | JA | ES |
|------|-----|-----|-----|
| `paywall_plan_cta_start_journey` | Start Journey | 旅を始める | Comienza el viaje |

---

### F7: Analytics guard

```diff
- if package.storeProduct.introductoryDiscount != nil {
+ if package.packageType == .annual && package.storeProduct.introductoryDiscount != nil {
```

---

### F8: PostHog SDK 統合 ✅ 完了

**SPM:** `posthog-ios` 3.0.0 を pbxproj に追加済み

**AppDelegate.swift:**

```swift
import PostHog
import RevenueCat

// didFinishLaunchingWithOptions 内、SubscriptionManager.configure() の後:
let phConfig = PostHogConfig(
    apiKey: "phc_Mw4K3aByYDRuAlfe55u5OYJrTwTcwhPextZjOw8z2nw",
    host: "https://us.i.posthog.com"
)
phConfig.sessionReplay = true
phConfig.sessionReplayConfig.maskAllTextInputs = true
phConfig.sessionReplayConfig.maskAllImages = false
PostHogSDK.shared.setup(phConfig)
PostHogSDK.shared.identify(Purchases.shared.appUserID)
```

| 項目 | 値 |
|------|-----|
| Project Token | `phc_Mw4K3aByYDRuAlfe55u5OYJrTwTcwhPextZjOw8z2nw` |
| Project ID | `327882` |
| Region | US Cloud |
| Host | `https://us.i.posthog.com` |

---

### F9: PaywallVariantBView.swift（CRO 最適化デザイン）

**新規ファイル:** `aniccaios/aniccaios/Onboarding/PaywallVariantBView.swift`

**決済バックエンドは 100% RevenueCat。PostHog は振り分け + analytics のみ。**

**設計原則:**
- 全価格テキスト: `package.localizedPriceString`（StoreKit 動的）
- トライアルテキスト: `introductoryDiscount.subscriptionPeriod`（StoreKit 動的）
- 日割り計算: `yearlyPrice / 365`（動的計算）
- **ハードコードされた金額・期間はゼロ**
- 価格変更 = ASC で変更するだけ。アプリ再提出不要

**⚠️ 機能コピーの検証（CRO skill: 「存在しない機能を書くことは詐欺」）:**

| コピー | 実際のコード | 検証 |
|--------|------------|------|
| "AI that learns YOUR patterns" | `LLMNudgeService` — Pro のみ AI 生成 nudge | ✅ 実在（Pro） |
| "👍/👎 feedback improves" | `NudgeFeedbackService` — hookFeedback "tapped" | ✅ 実在（Pro） |
| "3x daily mindful nudges" | Free: 3本固定 / Pro: 問題別最適時刻 | ✅ 実在（両方） |
| "Personalized to your struggle" | `ProblemType` 選択ベースで全ユーザー | ✅ 実在（両方） |
| "Cancel anytime" | Apple subscription standard | ✅ 事実 |

**CRO skill 引用マッピング:**

| 要素 | CRO skill セクション | 引用 |
|------|---------------------|------|
| Title: 感情的共鳴 | Headline | 「Focus on what they get, not what they pay: Unlock [Feature] to [Benefit]」 |
| Subtitle: 社会的証明 | Social Proof | 「Customer quotes about the upgrade / X teams use this feature」 |
| Feature list: 具体的ベネフィット | Value Demonstration | 「Show what they're missing / Specific to their use case if possible」 |
| Plan card: 日割り価格 | Pricing Presentation Experiments | 「Price per day framing ("Less than a coffee")」 |
| Plan card: BEST VALUE badge | Pricing Presentation Experiments | 「Add "Most Popular" badge to target plan」 |
| CTA: Trial 具体名 | CTAs Experiments | 「Value-specific ("Unlock Unlimited") vs. generic ("Upgrade")」 |
| Social proof: レビュー | Social Proof | 「Success metrics from upgraded users」 |
| Trust: 🔒 + 具体保証 | Objection Handling Experiments | 「Add money-back guarantee messaging / Show "Cancel anytime" prominently」 |
| Escape: Maybe later | Escape Hatch | 「Clear "Not now" / Don't make them feel bad」 |
| Default to annual | Plan Selection | 「Default to recommended plan」 |

**Variant B EN:**

```
┌───────────────────────────────────┐
│                              ✕    │  ← CRO: Escape Hatch「Clear dismiss」
│             🧘‍♂️                    │  ← CRO: Value Demo「Preview/visual」
│                                   │
│    You deserve to feel better     │  ← CRO: Headline「what they GET」
│                                   │
│    Join 1,000+ people on their    │  ← CRO: Social Proof「X teams use」
│    journey to change              │
│                                   │
│    ✅ AI that learns YOUR patterns │  ← CRO: Value Demo + 実機能(LLMNudgeService)
│    ✅ 3x daily mindful nudges     │  ← CRO: Value Demo + 実機能(3本/日)
│    ✅ Personalized to your struggle│  ← CRO: Value Demo + 実機能(ProblemType)
│    ✅ 👍/👎 feedback improves     │  ← CRO: Value Demo + 実機能(NudgeFeedback)
│    ✅ Cancel anytime, no questions │  ← CRO: Objection Handling
│                                   │
│   ┌─────────────────────────────┐ │
│   │ Annual          BEST VALUE  │ │
│   │ $49.99/yr                   │ │  ← localizedPriceString
│   │ Just $0.14/day              │ │  ← yearlyPrice/365
│   │ 🎁 Includes 1 week free     │ │  ← introductoryDiscount（有の時のみ）
│   │                        (●) │ │
│   └─────────────────────────────┘ │
│   ┌─────────────────────────────┐ │
│   │ Monthly                     │ │
│   │ $9.99/mo                    │ │  ← localizedPriceString
│   │                        ( ) │ │
│   └─────────────────────────────┘ │
│                                   │
│   ┌─────────────────────────────┐ │
│   │  Start Your Free Week Now ▶ │ │  ← Annual+trial有
│   └─────────────────────────────┘ │
│                                   │
│   ⭐ "This app saved my sleep     │
│       schedule"                   │
│                                   │
│   🔒 Cancel anytime. No charge    │  ← Annual+trial有の時のみ
│      until trial ends.            │
│                                   │
│   Maybe later    Restore          │
│   Terms · Privacy                 │
└───────────────────────────────────┘
```

**Variant B JA:**

```
┌───────────────────────────────────┐
│                              ✕    │
│             🧘‍♂️                    │
│                                   │
│    あなたは楽になっていい           │
│                                   │
│    1,000人以上が変化の旅を          │
│    歩んでいます                     │
│                                   │
│    ✅ 1日3回のマインドフルナッジ     │
│    ✅ あなたのパターンを学ぶAI      │
│    ✅ 悩みに合わせたパーソナライズ   │
│    ✅ 👍/👎 フィードバックで改善    │
│    ✅ いつでもキャンセル、質問なし   │
│                                   │
│   ┌─────────────────────────────┐ │
│   │ 年額プラン      おすすめ     │ │
│   │ ¥7,800/年                   │ │
│   │ 1日たったの¥21               │ │
│   │ 🎁 1週間無料体験付き         │ │
│   │                        (●) │ │
│   └─────────────────────────────┘ │
│   ┌─────────────────────────────┐ │
│   │ 月額プラン                  │ │
│   │ ¥1,000/月                  │ │
│   │                        ( ) │ │
│   └─────────────────────────────┘ │
│                                   │
│   ┌─────────────────────────────┐ │
│   │ 無料の1週間を今すぐ始める ▶  │ │
│   └─────────────────────────────┘ │
│                                   │
│   ⭐「睡眠リズムが救われた」        │
│                                   │
│   🔒 いつでもキャンセル可能。       │
│      トライアル中は課金されません。  │
│                                   │
│   あとで    復元                   │
│   利用規約 · プライバシー          │
└───────────────────────────────────┘
```

**Trust copy 動的切替（Monthly にはトライアルがないため嘘にならない）:**

| 選択プラン | 条件 | Trust EN | Trust JA |
|-----------|------|---------|---------|
| Annual | trial 有 | 🔒 Cancel anytime. No charge until trial ends. | 🔒 いつでもキャンセル可能。トライアル中は課金されません。 |
| Annual | trial 無 | 🔒 Cancel anytime. | 🔒 いつでもキャンセル可能。 |
| Monthly | — | 🔒 Cancel anytime. | 🔒 いつでもキャンセル可能。 |

**CTA 動的切替:**

| 選択プラン | 条件 | CTA EN | CTA JA |
|-----------|------|--------|--------|
| Annual | trial 有 | Start Your Free {period} Now ▶ | 無料の{period}を今すぐ始める ▶ |
| Annual | trial 無 | Start Your Journey Now ▶ | 今すぐ旅を始める ▶ |
| Monthly | — | Start Your Journey Now ▶ | 今すぐ旅を始める ▶ |

**Trial period テキスト生成（動的）:**

```swift
private var trialPeriodText: String? {
    guard let discount = selectedPackage?.storeProduct.introductoryDiscount else { return nil }
    let period = discount.subscriptionPeriod
    let isJA = Locale.current.languageCode == "ja"
    switch period.unit {
    case .day:   return isJA ? "\(period.value)日間" : period.value == 1 ? "1 Day" : "\(period.value) Days"
    case .week:  return isJA ? "\(period.value)週間" : period.value == 1 ? "Week" : "\(period.value) Weeks"
    case .month: return isJA ? "\(period.value)ヶ月" : period.value == 1 ? "1 Month" : "\(period.value) Months"
    case .year:  return isJA ? "\(period.value)年" : period.value == 1 ? "1 Year" : "\(period.value) Years"
    @unknown default: return nil
    }
}
```

**Variant B ローカライズキー（3言語: EN/JA/ES）:**

| キー | EN | JA | ES |
|------|-----|-----|-----|
| `paywall_b_title` | You deserve to feel better | あなたは楽になっていい | Mereces sentirte mejor |
| `paywall_b_subtitle` | Join 1,000+ people on their journey to change | 1,000人以上が変化の旅を歩んでいます | Únete a más de 1,000 personas en su camino al cambio |
| `paywall_b_feature_nudges` | 3x daily mindful nudges | 1日3回のマインドフルナッジ | 3 nudges conscientes al día |
| `paywall_b_feature_ai` | AI that learns YOUR patterns | あなたのパターンを学ぶAI | IA que aprende TUS patrones |
| `paywall_b_feature_personalized` | Personalized to your struggle | 悩みに合わせたパーソナライズ | Personalizado a tu lucha |
| `paywall_b_feature_feedback` | 👍/👎 feedback improves your care | 👍/👎 フィードバックでケアが改善 | 👍/👎 mejora tu cuidado |
| `paywall_b_feature_cancel` | Cancel anytime, no questions asked | いつでもキャンセル、質問なし | Cancela cuando quieras, sin preguntas |
| `paywall_b_daily_price` | Just %@/day | 1日たったの%@ | Solo %@/día |
| `paywall_b_trial_badge` | 🎁 Includes %@ free | 🎁 %@無料体験付き | 🎁 Incluye %@ gratis |
| `paywall_b_cta_trial` | Start Your Free %@ Now ▶ | 無料の%@を今すぐ始める ▶ | Empieza tu %@ gratis ahora ▶ |
| `paywall_b_cta_no_trial` | Start Your Journey Now ▶ | 今すぐ旅を始める ▶ | Comienza tu viaje ahora ▶ |
| `paywall_b_review` | "This app saved my sleep schedule" | 「睡眠リズムが救われた」 | "Esta app salvó mi horario de sueño" |
| `paywall_b_trust_trial` | 🔒 Cancel anytime. No charge until trial ends. | 🔒 いつでもキャンセル可能。トライアル中は課金されません。 | 🔒 Cancela cuando quieras. Sin cargos hasta que termine la prueba. |
| `paywall_b_trust_no_trial` | 🔒 Cancel anytime. | 🔒 いつでもキャンセル可能。 | 🔒 Cancela cuando quieras. |

---

### F10: PostHog feature flag で paywall A/B 振り分け

**PostHog Dashboard で作成する Experiment:**

| 項目 | 値 |
|------|-----|
| Feature Flag 名 | `paywall-ab-test` |
| Variant A | `control`（既存 PlanSelectionStepView） |
| Variant B | `test`（新 PaywallVariantBView） |
| Split | 50/50 |
| Goal Metric | `paywall_purchased` (PostHog custom event) |
| Secondary Metrics | `paywall_dismissed`, `paywall_viewed` |

**PostHog Dashboard セットアップ手順:**

```
1. https://us.posthog.com/project/327882 → Settings → Personal API Keys → Create
   → Name: "anicca-cli" → Create → コピー（これが Personal API Key）

2. PostHog MCP セットアップ:
   npx @posthog/wizard mcp add

3. Experiments → New Experiment
   → Name: "Paywall A/B Test v1"
   → Feature flag key: "paywall-ab-test"
   → Control: "control" (50%)
   → Test: "test" (50%)
   → Goal metric: "paywall_purchased"
   → Secondary: "paywall_dismissed"
   → Launch
```

**振り分けコード:** `OnboardingFlowView.swift` L94-103

```diff
  case .planSelection:
+     let variant = PostHogSDK.shared.getFeatureFlag("paywall-ab-test") as? String ?? "control"
+     if variant == "test" {
+         PaywallVariantBView(
+             variant: variant,
+             onPurchaseSuccess: { customerInfo in handlePaywallSuccess(customerInfo: customerInfo) },
+             onDismiss: { handlePaywallDismissedAsFree() }
+         )
+     } else {
          PlanSelectionStepView(
-             onPurchaseSuccess: { customerInfo in handlePaywallSuccess(customerInfo: customerInfo) },
-             onDismiss: { handlePaywallDismissedAsFree() }
+             variant: variant,
+             onPurchaseSuccess: { customerInfo in handlePaywallSuccess(customerInfo: customerInfo) },
+             onDismiss: { handlePaywallDismissedAsFree() }
          )
+     }
```

**PostHog イベント tracking（Mixpanel と並行 — Mixpanel は削除しない）:**

| PostHog イベント | いつ | properties | 対応 Mixpanel イベント |
|----------------|------|-----------|---------------------|
| `paywall_viewed` | paywall 表示時 | `variant` | `paywall_plan_selection_viewed`（variant なし） |
| `paywall_purchased` | RevenueCat 購入成功時 | `variant`, `plan_type`, `has_trial` | `onboarding_paywall_purchased`（variant なし） |
| `paywall_dismissed` | ✕ or Maybe later タップ時 | `variant` | `onboarding_paywall_dismissed_free`（variant なし） |

**Mixpanel は残す。** 既存の AppMetrics cron / OpenClaw スキルが Mixpanel から読んでる。PostHog は A/B テスト専用レイヤーとして追加。

**自動改善サイクル:**

```
Week 1: Variant A vs B → PostHog が統計的有意差を検出
  → Winner をベースに
  → テキスト1要素だけ変えた新 Variant を作る
  → Winner vs 新 Variant で新 Experiment
  → 繰り返し
  → paywall が自己改善（Screenshot PPO と同じ原理）
```

---

### F11: Nudge バリエーション拡張（14→60 × 13問題 × 3言語）

**現状:**

| 問題 | 現在のバリエーション数 | 3回/日で何日持つか |
|------|---------------------|------------------|
| staying_up_late | 21 | 7日 |
| 他12問題 | 各14 | 約5日 |

**目標:** 各問題 60 バリエーション → **2ヶ月（60日）ローテーション**

**追加数:**

| 問題 | 追加数 | 言語 |
|------|--------|------|
| staying_up_late | 39（21→60） | EN/JA/ES |
| 他12問題 | 各46（14→60） | EN/JA/ES |
| **合計** | **39 + 46×12 = 591 バリエーション** | **× 3言語 × 2（notification+detail）= 3,546 文字列** |

**各バリエーションの構造（notification + detail ペア）:**

```
通知（ロック画面）:
  Title: problem_X_notification_title（F1 で翻訳済み）
  Body:  nudge_X_notification_N（hook テキスト）

カード（アプリ内）:
  Hook:   nudge_X_notification_N
  Detail: nudge_X_detail_N（行動科学ベースの具体的アドバイス）
```

**2ヶ月ローテーション:**

| 週 | バリエーション | 備考 |
|----|-------------|------|
| 1-2 | 1-14 | 既存 |
| 3-4 | 15-28 | 新規 |
| 5-6 | 29-42 | 新規 |
| 7-8 | 43-56 | 新規 |
| 9+ | 57-60 + ループ開始 | 新規 |

**品質基準:** EN は行動科学ベースで作成 → JA/ES は翻訳。全バリエーション具体的アクション付き。

---

## 3. Variant A 詳細（既存 PlanSelectionStepView 改善後）

**Variant A = 既存デザインに F5/F6/F7 パッチのみ適用。デザイン変更なし。**

**CRO skill 引用マッピング（Variant A の弱点分析）:**

| 要素 | 現状 | CRO skill の指摘 | 改善？ |
|------|------|-----------------|--------|
| Title | "Your path to change starts here" | Headline「Focus on what they GET」 | ❌ A では変えない（B で改善） |
| Feature list | 3個、汎用的 | Value Demo「Specific to their use case」 | ❌ A では変えない |
| Social proof | なし | Social Proof「Customer quotes」 | ❌ A では変えない |
| 日割り価格 | なし | Pricing「Price per day framing」 | ❌ A では変えない |
| Trust copy | 汎用 | Objection Handling「Cancel anytime prominently」 | ❌ A では変えない |
| **→ これらの弱点を B で全て改善。A/B テストで B の改善効果を測定。** | | | |

### Variant A EN

```
┌───────────────────────────────────┐
│                              ✕    │  ← CRO: Escape Hatch ✅
│                                   │
│   Your path to change starts here │  ← 既存タイトル（変更なし）
│                                   │
│   Start your journey to change    │  ← F5修正
│                                   │
│   ✅ Full access to all features  │  ← 既存（汎用的 — B で改善）
│   🔔 Personalized nudges          │  ← 既存
│   ✕  Cancel anytime               │  ← CRO: Objection Handling ✅
│                                   │
│   ┌─────────────────────────────┐ │
│   │ Annual Plan    BEST VALUE   │ │
│   │ {price}/yr      Save {pct}% │ │  ← 全て StoreKit 動的
│   │                        (●) │ │
│   └─────────────────────────────┘ │
│   ┌─────────────────────────────┐ │
│   │ Monthly Plan                │ │
│   │ {price}/mo                  │ │
│   │                        ( ) │ │
│   └─────────────────────────────┘ │
│                                   │
│   ┌─────────────────────────────┐ │
│   │ Start Free Trial            │ │  ← Annual + trial有
│   │ Start Journey               │ │  ← F6: Monthly選択時
│   └─────────────────────────────┘ │
│                                   │
│   No commitment. Cancel anytime.  │
│   Maybe later    Restore          │
│   Terms · Privacy                 │
└───────────────────────────────────┘
```

### Variant A JA

```
┌───────────────────────────────────┐
│                              ✕    │
│                                   │
│   変わるための道は、ここから        │
│   変化への旅を始めよう             │  ← F5修正
│                                   │
│   ✅ 全機能にアクセス              │
│   🔔 パーソナライズされたナッジ     │
│   ✕  いつでもキャンセル             │
│                                   │
│   ┌─────────────────────────────┐ │
│   │ 年額プラン      おすすめ     │ │
│   │ {price}/年       {pct}%お得  │ │
│   │                        (●) │ │
│   └─────────────────────────────┘ │
│   ┌─────────────────────────────┐ │
│   │ 月額プラン                  │ │
│   │ {price}/月                  │ │
│   │                        ( ) │ │
│   └─────────────────────────────┘ │
│                                   │
│   ┌─────────────────────────────┐ │
│   │ 無料トライアルを始める        │ │  ← Annual + trial有
│   │ 旅を始める                   │ │  ← F6: Monthly選択時
│   └─────────────────────────────┘ │
│                                   │
│   いつでもキャンセル可能。          │
│   あとで    購入を復元             │
│   利用規約 · プライバシー          │
└───────────────────────────────────┘
```

---

## 4. 動的テキスト一覧（ハードコードゼロ）

| テキスト | ソース | 例（EN） | 例（JA） |
|---------|--------|---------|---------|
| 価格（年額） | `package.localizedPriceString` | $49.99 | ¥7,800 |
| 価格（月額） | `package.localizedPriceString` | $9.99 | ¥1,000 |
| Save % | `1 - yearly / (monthly * 12)` | Save 58% | 58%お得 |
| 日割り価格 | `yearlyPrice / 365` | $0.14/day | ¥21/日 |
| Trial 期間 | `introductoryDiscount.subscriptionPeriod` | 1 week | 1週間 |
| Trial CTA | `subscriptionPeriod` → 動的テキスト | Start Your Free Week Now | 無料の1週間を今すぐ始める |
| Trial badge | `introductoryDiscount != nil` | 🎁 Includes 1 week free | 🎁 1週間無料体験付き |
| Trust copy | `introductoryDiscount != nil` で分岐 | No charge until trial ends. | トライアル中は課金されません。 |

**価格変更フロー: ASC で変更 → StoreKit 自動配信 → アプリ再提出ゼロ**

---

## 5. 実行順序

```
Phase 1: PostHog SDK 統合（F8）✅ 完了
  ├── SPM 追加 ✅
  ├── AppDelegate setup + identify ✅
  ├── Session Replay 有効化 ✅
  ├── PostHog Personal API Key 作成 ✅ phx_MD5o...
  ├── PostHog MCP セットアップ ✅ claude mcp add posthog
  ├── PostHog Feature Flag 作成 ✅ ID: 628062, key: paywall-ab-test, 50/50
  └── PostHog Experiment 作成 ✅ ID: 364239, "Paywall A/B Test v1", LAUNCHED

Phase 2: Paywall A/B テスト実装（F5, F6, F7, F9, F10）← 最優先
  ├── F5: subtitle 修正（3言語: EN/JA/ES）
  ├── F6: Monthly CTA 変更（Swift + 3言語）
  ├── F7: Analytics guard（Swift L272）
  ├── F9: PaywallVariantBView.swift 新規作成
  ├── F9: Variant B ローカライズ（14キー × 3言語 = 42文字列）
  ├── F10: feature flag 振り分けコード（OnboardingFlowView.swift）
  └── F10: PostHog event tracking 3種（paywall_viewed/purchased/dismissed）

Phase 3: UX 改善 + ローカライズ（F1, F2, F3, F4）
  ├── F1: 通知タイトル翻訳（13キー × 4言語 = 52文字列）
  ├── F2: スワイプ削除（AppState + MyPathTabView + 6言語）
  ├── F3: タブタイトル（6言語）
  └── F4: JA アプリ名

Phase 4: Nudge 拡張（F11）
  ├── staying_up_late: 39 バリエーション追加 × 3言語 × 2 = 234文字列
  └── 他12問題: 46 バリエーション追加 × 3言語 × 2 = 3,312文字列
      合計: 3,546 文字列

Phase 5: ビルド + 提出
  ├── fastlane build
  ├── App Store 提出
  └── ASC JA name "アニッチャ" メタデータ更新
```

---

## 6. 不確実な点（全解決済み ✅）

| # | 点 | 結果 |
|---|---|------|
| 1 | PostHog SDK `sessionReplay` API 名がv3で正しいか | ✅ `PostHogConfig.swift` ソース確認済み。`phConfig.sessionReplay = true` が正しい |
| 2 | `getFeatureFlag()` 戻り値が `String` cast 可能か | ✅ `Any?` を返す。`as? String ?? "control"` で安全にフォールバック |
| 3 | feature flag がオンボーディング到達時にロード済みか | ✅ `preloadFeatureFlags` デフォルト `true`。setup 時に自動ロード |
| 4 | `Purchases.shared.appUserID` が AppDelegate 時点で利用可能か | ✅ `SubscriptionManager.configure()` が先に呼ばれる。anonymous ID が即利用可能 |
| 5 | pbxproj 手動編集で Xcode が壊れないか | ✅ 既存パッケージと同一パターンで追加済み。ビルドで最終確認 |
| 6 | `getFeatureFlagPayload()` でテキスト変更可能か | ✅ 存在確認済み。将来のアプリ再提出なしテキスト変更に使用可能 |
| 7 | Mixpanel/PostHog/RC のデータ関係 | ✅ PostHog=A/B振り分け+analytics、RC=決済、Mixpanel=既存cron維持。全並行 |

---

## 7. 境界（やらないこと）

| やらないこと | 理由 |
|-------------|------|
| Superwall SDK 統合 | PostHog A/B に方針変更 |
| Mixpanel 削除 | 既存 AppMetrics cron が依存。並行運用 |
| 価格テスト用プロダクト作成 | A/B テスト結果後に検討 |
| iPad レイアウト | 別スペック |
| Backend API 変更 | iOS のみ |

---

## 8. テスト

### Unit Tests

| # | テスト | カバー |
|---|--------|--------|
| 1 | `testRemoveProblemUpdatesStruggles()` | F2 |
| 2 | `testRemoveNonexistentProblemIsNoOp()` | F2 |
| 3 | `testPaywallVariantBDailyPriceCalculation()` | F9 |
| 4 | `testPaywallVariantBTrialTextDynamic()` | F9 |

### 手動確認

| # | 確認 | カバー |
|---|------|--------|
| 1 | JA デバイスでアプリ名 "アニッチャ" | F4 |
| 2 | Monthly 選択時 CTA "Start Journey" | F6 |
| 3 | PostHog feature flag で Variant B 表示 | F10 |
| 4 | Variant B の日割り価格が動的 | F9 |
| 5 | Variant B の trial テキストが StoreKit から取得 | F9 |
| 6 | Session Replay が PostHog Dashboard で確認可能 | F8 |
| 7 | 購入が RevenueCat 経由で成功 | F9 |
| 8 | Trust copy が Monthly 選択時に trial 文言を含まない | F9 |

### E2E 判定

**E2E 不要（理由）:** paywall の A/B テストは onboarding フロー内で表示される。onboarding E2E は既に Maestro テストがある。新規 E2E は PostHog feature flag のモック（`-forcePaywallVariantB` launch argument）を使った手動確認で代替可能。

---

*最終更新: 2026-04-01 v4（CRO skill引用、PostHog全完了、実行順序変更、不確実点全解決）*
