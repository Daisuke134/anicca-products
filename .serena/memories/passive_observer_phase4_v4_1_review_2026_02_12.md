Reviewed specs/passive-observer-agent.md Phase 4 v4.1 against prior blockers. Confirmed fixed: dynamic plist generation in install.sh (no launchd dir dependency), individual launchctl load/unload commands (no glob), sync script has remote mkdir -p and success/failure logging, and use of $HOME for portability. Noted advisory: install.sh verifies required files observer.sh and shell-watcher.sh before copy, but sync-to-vps.sh copy remains optional via 'cp ... 2>/dev/null || true' without explicit pre-check, while sync plist is still generated and load attempted with '|| true'.