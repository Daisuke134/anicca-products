2026-02-10 (UTC) SlackでAniccaと話せない調査。原因は複合: (1) 端末上で `systemctl`/`journalctl`/`ss` を叩いて `command not found` になっていたが、これはVPSではなくmacOSローカルで実行していたため。 (2) VPSの `openclaw-gateway.service` は active だが、OOM killer によりkill→再起動を繰り返した痕跡あり。 (3) gatewayログに `Error: Unknown model: openai-codex/gpt-5.3-codex` が多数。`~/.openclaw/openclaw.json` の primary は `openai-codex/gpt-5.3-codex` へ切替済みだが、OpenClaw側でモデル名が認識されていない/認証未完了/プロバイダ未導入などで実行失敗している可能性が高い。暫定復旧は model を `openai/gpt-4o` に戻して gateway restart。恒久対応は (a) VPSで `openclaw models auth login --provider openai-codex` を完了 (OAuth) (b) `openclaw models status/list` で `openai-codex/gpt-5.3-codex` が利用可能として見えることを確認してから primary を切替 (c) OAuth中は gateway を止めてOOM回避、必要ならswap/メモリを増やす。